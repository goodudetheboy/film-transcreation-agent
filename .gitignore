node_modules/
dist/
build/
coverage/
.env
.env.local
*.local
.DS_Store
*.log
npm-debug.log*

# Python (test_agent.py reference script)
__pycache__/
*.pyc
.venv/
venv/

# Firebase local cache/logs, and any accidentally-generated deploy keys
.firebase/
firebase-debug.log
*-key.json

# Local film persistence (see docs/adr/0016) — not committed, dev-machine only
backend/.data/

# Integration tests' own mock-upload artifacts (multer diskStorage writes real
# files under here when films.integration.test.ts runs) — previously untracked
# but uncovered, so a stray run could get committed by accident; see 20260828's
# progress notes.
tests/integration/.data/
