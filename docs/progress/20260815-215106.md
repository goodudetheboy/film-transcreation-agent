# 2026-08-15 21:51 — Settings dropdown, UI rename

## What changed

- Moved the "Test mode" checkbox out of the inline form and into a new
  `HeaderSettings` component — a "Settings" button in the top-right of the header
  that toggles a dropdown panel, per user request ("global setting button... in the
  top bar"). Built test-first (`HeaderSettings.test.tsx`): button renders, panel
  closed initially, opens/closes on repeated clicks, checkbox reflects the current
  `testMode` prop, `onTestModeChange` fires correctly. `App.tsx` now owns `testMode`
  state and passes it down; the form itself no longer has the checkbox.
- Renamed the UI-visible product name from "Cultural Resonance Agent" to
  "Film Transcreation Agent" (the `<h1>` in `App.tsx` and `<title>` in
  `frontend/index.html`) — matches the repo's actual name. Scoped to the two
  directly user-visible surfaces only; left "Cultural Resonance Agent" as-is in
  `docs/product/pitch.md`/`README.md`/ADRs, since that's the hackathon pitch's own
  chosen branding for the business-context documents, not the live app UI. If the
  rename should extend further (e.g. into the pitch/README framing too), that's a
  follow-up to confirm.
- Verified live in the browser: settings button opens/closes the panel correctly,
  checked-by-default test mode still works from its new location, title bar and
  header both show the new name.

## Decisions made

- No new ADR — this is UI layout/naming, not an architectural decision.

## Open TODOs / what's blocked

- Whether "Film Transcreation Agent" should also replace "Cultural Resonance Agent"
  in `docs/product/`, `README.md`, and the ADRs is unconfirmed — left untouched
  pending clarification.
- Everything else unchanged from the prior entry (local ADC, provisional
  `FlaggedLine` schema, `pitch.md` TODOs).
- This entry's changes plus the prior test-mode entry's changes are both still
  uncommitted — shipping together next.
