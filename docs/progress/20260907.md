# 2026-09-07

## 00:05 — Video-sight for Research chat; real run status on all three Agent badges

Picked up two gaps the user asked about after a research pass over the
Agent/Session chat system: why the Research chat agent couldn't look at video,
and why the Agent Status tab's "done" badge didn't reflect a still-running
batch pass. Planned via `EnterPlanMode`/`ExitPlanMode`, confirmed via
`AskUserQuestion` that the second fix should cover all three badge locations
(Agent Status tab plus both chat panels' own session-switcher lists), not just
the tab.

**Part 1 — `describe_video_segment` for Research chat.** Yesterday's 19:05
entry added this tool to the Discovery chat agent only, framed as groundwork
for a Discovery-specific backfill feature — Research chat never got it, even
though `Project.sourceFilmId` makes it just as feasible there.
[researchChatAgent.ts](../../backend/src/services/researchChatAgent.ts) gained
the same tool declaration/executor Discovery already has (`filmStore`/
`videoSegmentDescriber` added to `ResearchChatAgentDeps`, a new `filmId?`
field on `RunTurnInput`). The `POST .../chat-sessions/:sessionId/messages`
route in [projectChat.ts](../../backend/src/routes/projectChat.ts) resolves
`project.sourceFilmId` via the already-available `projectStore` and passes it
through — the agent never re-fetches the Project itself.
[mockResearchChatAgent.ts](../../backend/src/services/mockResearchChatAgent.ts)
threads the two new deps for type consistency only, same treatment
`mockDiscoveryChatAgent.ts` got. `server.ts`/`app.ts` wiring required hoisting
`filmStore`/`videoSegmentDescriber`'s construction above where
`createResearchChatAgent`/`createMockResearchChatAgent` are called (previously
declared after, since only Discovery needed them before). Frontend:
[ResearchChatPanel.tsx](../../frontend/src/components/ResearchChatPanel.tsx)'s
`ToolCallCard` gained a `describe_video_segment` case mirroring Discovery's
"Looking at mm:ss–mm:ss…" label (in-flight) / "Looked at mm:ss–mm:ss" +
description text (completed) — adapted rather than copied verbatim, since
Research's card structurally distinguishes pending vs. completed state while
Discovery's doesn't.

**Part 2 — real run status on Agent Status badges.** `ChatSessionStatus`/
`DiscoveryChatSessionStatus` (`'idle'|'streaming'|'error'`) only tracks
whether a chat reply is actively streaming — it flips back to `idle` the
instant a turn's reply finishes, saying nothing about a `DiscoveryJob`/
`ResearchRun` batch pass the session kicked off, which keeps processing
in the background via `discoveryQueueWorker.ts`/`projects.ts`'s batch loop.
New [agentRunStatus.ts](../../frontend/src/utils/agentRunStatus.ts):
`collectRunRefs(turns)` pulls every jobId/runId a session's turns ever logged
via a `run` marker part, and `combinedAgentStatus(chatStatus, runStatuses)`
folds them together — aggregating over every run a session ever kicked off
(not just the latest), so one still-queued run among several keeps the row
reading "running". Wired into all three places this badge renders:
[FilmAgentsTab.tsx](../../frontend/src/components/FilmAgentsTab.tsx) (now
also fetches `listDiscoveryJobs(filmId)` and folds `listResearchRuns(p.id)`
into its existing per-project batch — no new N+1, `AgentRow.status` simplified
to hold the already-combined value, `statusModifier`/`statusLabel` deleted),
and the session-switcher lists inside
[DiscoveryChatPanel.tsx](../../frontend/src/components/DiscoveryChatPanel.tsx)
and [ResearchChatPanel.tsx](../../frontend/src/components/ResearchChatPanel.tsx)
(each fetches its own job/run status list once alongside its existing session
list fetch). No backend changes needed — `listDiscoveryJobs`/`listResearchRuns`
already existed and already return `status`.

No new ADR for either part — both additive within existing patterns (ADR-0025
for the chat-agent-with-tools design, ADR-0026-adjacent for run status).

Verified: `npm run test` — 274 backend (2 new: `describe_video_segment` happy
path + too-long-range rejection on the research agent, mirroring Discovery's
existing tests), 77 frontend (one existing test,
`ProjectItemView.test.tsx`, needed a new `listResearchRuns` mock —
`ResearchChatPanel` is docked inside that view and the module is auto-mocked
there), 12 integration, all passing. Both `tsc -b`/`tsc -p tsconfig.app.json`
and `oxlint .` clean (only pre-existing, unrelated warnings). Live-walked
both parts end-to-end via the dev servers with real ADC credentials (test
mode off): sent a real message to a fresh Research session asking about food
visible at 00:33–00:36, confirmed `describe_video_segment` was actually
called against the real GCS-hosted video, watched it return "a plate of
fried chicken and a side salad, a dark red goblet, and a lit candle" — a real
Gemini video read, not mocked — and confirmed the "Looked at 00:33–00:36" tool
card rendered correctly with the description inline. Separately, in test
mode, created a Discovery job directly via the API without ever sending a
chat message (so the session's own `status` stayed `idle` throughout) and
confirmed the Agent Status tab showed a "running" badge (plus "1 running now"
in the header) while the mock pass was still processing, then "done" once it
completed — reproduced the same fix in `DiscoveryChatPanel`'s own
session-switcher list too. Deleted all test sessions/jobs created for this
afterward.

Shipped straight to `main` per explicit user direction.

## 01:15 — Five follow-up fixes: resizable agent panel, live status patching, testMode persistence, country context, Project tab label

A batch of small, independently-requested fixes picked up in the same session.

**1. Resizable docked "Agent" chat panel.** The Discovery/Research chat panel
docked beside the Details/Project table (`.workspace-details__chat` in
[FilmWorkspaceView.tsx](../../frontend/src/views/FilmWorkspaceView.tsx),
`.project-panel__chat` in
[ProjectPanel.tsx](../../frontend/src/components/ProjectPanel.tsx)) had a
hardcoded 420px width — user wanted drag-to-resize like the outer left/video
split already has. New
[useResizableChatPanel.ts](../../frontend/src/utils/useResizableChatPanel.ts)
(shared by both call sites via one `workspace.chatPanelWidth` localStorage
key — resizing either "Agent" panel remembers one width) plus a new
`.chat-panel-divider` CSS class mirroring `.workspace__divider`'s look, with
`margin: 0 calc(-1 * var(--space-loose))` to cancel the flex row's own `gap`
on both sides (inserting a third flex child would otherwise double the visual
spacing on top of the chat panel's existing border-left/padding-left).

User then reported the drag felt laggy — root cause: every pointermove ran
through `setWidth` (React state), re-rendering the whole panel subtree
(a live chat thread, potentially many turns) up to ~60×/sec. Fixed by having
the hook expose a `panelRef` the caller attaches to the wrapper div;
`onPointerMove` now mutates `panelRef.current.style.flexBasis` directly on
the DOM instead of via state, and `setWidth`/localStorage-persist only fire
once, on pointer-up, syncing React to the value already on screen (no visual
jump).

**2. Agent Status badges didn't update live.** Follow-up to this morning's
work: the badges reflected a snapshot fetched once at mount, so kicking off a
run and checking the badge without leaving the page (or the whole page
reload) still showed "done" until a fresh mount re-fetched. Root cause of one
confusing false-negative while testing this: Fast Refresh (Vite HMR) mid-edit
silently broke an already-open SSE subscription without a real remount — not
a logic bug, confirmed by retesting cleanly with no further file edits during
the test.
[FilmAgentsTab.tsx](../../frontend/src/components/FilmAgentsTab.tsx) now
subscribes (via the existing `streamDiscoveryJob`/`streamResearchRunUpdates`
resumable endpoints, same primitive the chat panels' in-thread run cards
already used) to every non-terminal job/run found in its initial fetch,
patching just that id's status in a `liveStatusById` map as events arrive; an
already-terminal job/run needs no subscription. `DiscoveryChatPanel.tsx`/
`ResearchChatPanel.tsx`'s own session-switcher badges got a smaller fix:
prefer the panel's existing `jobDetails`/`runDetails` (already live for any
session made active this mount) over the once-fetched snapshot.
Live-verified: kicked off a run with the session's own chat status staying
`idle` throughout (no message sent), watched the badge read "running" the
instant the run was logged and flip to "done" in place once the mock pass
actually finished, no reload or navigation.

**Mistake made while testing this**: cleaning up test data afterward, deleted
a session named "Testing agentic discovery" that had existed *before* this
testing pass started (not one this session created) — it should not have
been deleted, and Firestore deletes here have no undo. Flagged to the user.

**3. Test Mode toggle wasn't persisted.** `App.tsx`'s `testMode` was a bare
`useState(true)`, resetting to the default every reload. New
[useTestMode.ts](../../frontend/src/utils/useTestMode.ts), same shape as the
existing [useTheme.ts](../../frontend/src/utils/useTheme.ts) (`localStorage`
key `testMode`, default `true` — same safe "mock data" default as before —
when nothing is stored).

**4. Research chat had no idea what country it was localizing for.**
`researchChatAgent.ts`'s system instruction never mentioned
`project.country` at all — despite `search_web`'s own tool description
referring to "the target country" as if the model already knew it. The batch
`researchAgent.ts` already threads `project.country` into its prompt as
`target_country` (see `routes/projects.ts`); mirrored that for chat — new
`country?: string` on `RunTurnInput`, a `buildCountryContext()` block
prepended to the system instruction, resolved by
[projectChat.ts](../../backend/src/routes/projectChat.ts)'s messages route
from the `Project` doc it already fetches for `filmId`. Live-verified with a
real Gemini call: asked a fresh Research session "what country are we
localizing this project for, exactly?", got back "Vietnam."

**5. Project tab label.** The workspace nav's "Project" tab now reads
"Project: 🇻🇳 Vietnam" (flag + country name) once a project is selected,
instead of the bare word "Project" — `FilmWorkspaceView.tsx` derives
`selectedProject` from `filmProjects`/`projectId` and swaps the label.

No new ADRs — all five are additive UI/wiring fixes within existing
patterns, no architectural decisions.

Verified: `npm run test` — 275 backend (1 new: research chat country-context
test), 77 frontend, 12 integration, all passing. Both `tsc -b`/`tsc -p
tsconfig.app.json` and `oxlint .` clean (only pre-existing, unrelated
warnings). All five live-walked via the dev servers as described above.

Shipped straight to `main` per explicit user direction.

## 01:40 — Follow-up: the ref-based drag fix above still lagged

User reported dragging the agent panel divider still felt ~0.5s behind the
cursor even after the ref-based DOM-mutation fix, unlike the outer
left/video `workspace__divider` (which sticks to the cursor despite going
through React state on every pointermove). The real cause: `.workspace-
details__chat`/`.project-panel__chat`'s CSS has `transition: flex-basis
0.25s ease` for the smooth open/close animation — left active during a drag,
every mousemove's new `flex-basis` value got eased toward instead of applied
instantly, so the panel visibly chased the cursor by roughly that
transition's duration. The outer divider has no such transition on
`gridTemplateColumns`, which is why it was never affected regardless of the
state-vs-ref approach — the earlier "re-render is the bottleneck" diagnosis
was real but incomplete.

Fixed in
[useResizableChatPanel.ts](../../frontend/src/utils/useResizableChatPanel.ts):
`onPointerDown` now also sets `panelRef.current.style.transition = 'none'`,
restored via `removeProperty('transition')` on `onPointerUp` so the class-
based transition resumes for the next open/close toggle. Verified the
transition is genuinely `0s` during a drag and back to `0.25s` after release
via a dispatched pointer-event sequence in the browser console; user
confirmed live in their own browser that the panel now sticks to the cursor.

Shipped straight to `main` per explicit user direction.

## 02:15 — Redesigned the /films/new/preparing screen — first beat of the demo video

User flagged the upload/prep screen as "looks like dogshit" and asked for a
real visual pass, since this is the first thing the demo video shows: content
pinned to the top of an otherwise empty page (no vertical centering), a
static "Your film is being prepared" heading that never changed, a 15px
stage caption doing all the actual communicating, bare unlabeled dots for
progress, and no progress indicator at all once the film exists (only the
pre-creation video-upload step ever showed one).

Full pass on [FilmPreparingView.tsx](../../frontend/src/views/FilmPreparingView.tsx)
and its CSS (new block in [index.css](../../frontend/src/index.css), replacing
the old `.prep-steps`/bare-dot rules — `.prep-stage-label` kept, still used by
the dev-only `PrepAnimationLab`):

- **New `.prep-hero` wrapper** — `flex:1` + `justify-content:center` so the
  screen is genuinely vertically centered in `.app-body`'s available height,
  not just horizontally like the old `.app-body-inner--centered`. A soft
  blurred radial-gradient glow (`.prep-hero__glow`) sits behind the Lottie
  animation — accent-tinted normally, tinted danger/success on error/ready —
  so the screen reads as one deliberate focal moment instead of a small
  animation floating in a mostly-empty page.
- **The big headline is now the dynamic status text** — per the user's direct
  ask, `STAGE_LABELS[displayStage]` (already good copy, e.g. "Uploading your
  video…", "Searching the video for details…") is now the actual `<h1>`
  (32px, keyed by stage so each change fades/rises in via a new
  `prepHeadlineIn` keyframe), not a static "Your film is being prepared"
  with the real status buried in a 15px caption below it. A small constant
  eyebrow ("PREPARING YOUR FILM") stays above it for context, and a new
  `STAGE_HINTS` map (`useStageDwell.ts`) adds one reassuring supporting line
  under the headline per stage.
- **Stepper redesigned** from bare 8px dots into a labeled, connected
  `.prep-stepper` (Video/Script/Details/Finalize, via a new `STEP_LABELS`
  map) — done steps green, the active one accent-colored with a soft glow
  ring, upcoming ones dim, joined by a single connecting rail.
- **Every in-progress stage now shows a progress indicator**, not just the
  video upload — new `.prep-progress` custom element (replacing the native
  `<progress>`, dropped since nothing else needed styling flexibility)
  renders determinate with a live percentage for the one stage that has real
  numbers (video upload) and a sliding indeterminate fill (new
  `prepProgressIndeterminate` keyframe) for every other in-flight stage
  (subtitle upload, discovery, finalizing) — previously those showed no
  progress motion at all once the animation itself wasn't obviously "new."
  Hidden entirely once ready or errored.
- **Error state gets a way out** — a small "← Back to Films" link now
  appears under the error message; previously a post-creation prep failure
  left the "Start Creating" button permanently disabled with no path forward
  at all, a real dead end that would have been bad to hit live in the demo.
- **Removed the Activity log** entirely per direct follow-up feedback ("this
  is pretty trash") — it added an expandable technical timestamp/message
  list under a status screen that didn't need one.
- `.upload-progress` (the old native-`<progress>` styling) deleted — grepped
  first to confirm nothing else referenced it once the custom
  `.prep-progress` replaced its one call site.

No new ADR — this is a single-screen visual redesign, not an architectural
decision.

Verified: `npx tsc -p tsconfig.app.json --noEmit` and `oxlint .` both clean;
`npx vitest run` from `frontend/` (matters here — running it from the repo
root instead picked up a stray `.claude/worktrees/agent-.../frontend` left
by an unrelated past agent session and produced 53 false failures from that
worktree's own stale test files, not this change) — 77/77 passing, including
`FilmPreparingView.test.tsx`'s two stage-text assertions, unaffected since
they assert by visible text, not the class names that moved. Live-walked
the whole sequence via the dev servers in test mode (a throwaway film,
deleted after): video upload with a real determinate percentage, into
discovery_running's labeled stepper with the indeterminate bar sliding,
into the ready state (green-tinted glow, stepper/progress correctly hidden,
"Start Creating" enabled, Activity log confirmed gone), and checked the
mobile breakpoint (375px) — eyebrow, headline, and button all render
cleanly with no overflow.

Shipped straight to `main` per explicit user direction.

## 02:35 — Mention chip overflow + visibility

User flagged (with a screenshot) a reference chip overflowing horizontally
out of its chat bubble — a `@`-mention pill referencing a Detail row spilled
past the bubble and even past the panel's left edge, clipped mid-word. Root
cause in [chatReferences.ts](../../frontend/src/utils/chatReferences.ts) and
[index.css](../../frontend/src/index.css): `chipDisplayText()`'s label
truncation was capped at 40 characters, which combined with the "Detail
HH:MM–HH:MM: " prefix produces a ~65-70 char string; `.mention-chip` sets
`white-space: nowrap` with no `max-width`, so at typical chat-panel widths
(the now-resizable panel from earlier today defaults to 420px, easily
narrower in practice) that string doesn't fit and the unbreakable chip
just overflows its container. Fixed both the trigger and added a hard
safety net: `MAX_LABEL_LENGTH` 40 → 26 (short enough to fit comfortably at
common panel widths — the label is for human identification only, the model
gets full row context separately per turn, so a shorter label loses nothing
functionally), and `.mention-chip` gained `max-width: 100%` +
`text-overflow: ellipsis` so even a still-too-long chip in an unusually
narrow panel truncates cleanly instead of overflowing.

Same conversation, user separately asked to make the chip a distinguishable
blue — turned out to be a real contrast bug, not just a nice-to-have:
`.mention-chip` used `background: var(--accent-dim)`, the *exact* background
`.chat-bubble--user` itself uses, so a chip inside a user message was nearly
invisible (only its equally-dim border set it apart). Changed to solid
`var(--accent)` background with `var(--accent-on)` (white) text — reads
clearly against both the user bubble's `--accent-dim` and the model bubble's
`--panel-2`, in both themes (neither `--accent`/`--accent-on` is
theme-overridden).

No new ADR — CSS/constant tuning within the existing mention-chip design
(the drag-and-drop chip system from earlier this week). Verified:
`npx tsc -p tsconfig.app.json --noEmit`, `oxlint`, and `npx vitest run`
(chatReferences.test.ts's truncation test only asserts "contains an ellipsis,
doesn't contain the full string" — not the exact cutoff length, so it stayed
green) all clean, 77/77 frontend tests passing. Live-verified via the dev
servers: sent a message embedding a long detail-reference token directly
through the (mock) messages API, confirmed the redisplayed chip truncates
with an ellipsis and renders as a clearly visible solid-blue pill, then
force-narrowed the chat panel to 340px via the console and confirmed the
same chip still truncates cleanly with no overflow at any width.

Shipped straight to `main` per explicit user direction.

## 02:55 — Trimmed the search_web result card; agent now cites sources it finds

User flagged (with a screenshot) the `search_web` tool-result card as way
too long — it rendered each result's raw first excerpt in full, including
unrendered markdown link/reference syntax straight from Parallel's response
(`[[2]]()`, `[text](url)`), turning one search into a wall of text.
[ResearchChatPanel.tsx](../../frontend/src/components/ResearchChatPanel.tsx)'s
`.chat-search-result` card now shows only the title as a link — the excerpt
paragraph and the now-unused `excerpts` field on `SearchResultCard` are
gone, along with the now-dead `.chat-search-result__excerpt` CSS rule.

Same conversation, user asked for a related but separate fix: the agent's
own reply text after a search wasn't reliably citing where it got its
information — with the excerpt dump removed from the UI, a source the model
didn't explicitly link back to in prose would be invisible to the user
entirely. `researchChatAgent.ts`'s `SYSTEM_INSTRUCTION` gained an explicit
instruction: after a `search_web` call, the reply must cite its source(s) as
real markdown links using the exact URLs from that call's results, not
paraphrased or invented ones. `ChatMarkdown.tsx` already renders
`react-markdown` + `remark-gfm`, so a real `[text](url)` in the model's
output was already going to render as a clickable link — the gap was purely
the model not being told to write one.

No new ADR — a UI trim and a prompt addition within the existing chat-agent
design, not an architectural change. Verified: `tsc -b`/`tsc -p
tsconfig.app.json`, `oxlint`, and both `npm run test:frontend` (77/77) and
`npm run test:backend` (275/275) clean — no test referenced the removed
excerpt markup or asserted the old system-instruction text. Live-verified
with a real (non-test-mode) message via the dev servers, real ADC + a real
Parallel API key: asked the research chat "how would 'body count' land with
a Vietnamese audience", inspected the persisted turns directly — the model
called `search_web`, got back real Grokipedia/Kids360 results, and its final
reply text genuinely embedded `[Grokipedia](https://grokipedia.com/page/...)`-
style links using the real URLs from those results (not fabricated) — then
confirmed in the UI that the result card shows only compact link titles and
that those citation links render as real clickable text inline in the
reply. Test session deleted afterward.

Shipped straight to `main` per explicit user direction.

## 03:20 — Resizable agent panel in the item detail view too; open/closed state now persists

Two follow-ups from the same conversation as today's earlier resizable-panel
work.

**1. `ProjectItemView.tsx` never got the resizable/draggable treatment** — it
has its own docked `.project-item-view__chat` (same "table + sliding agent
panel" mechanism as `.workspace-details__chat`/`.project-panel__chat`,
per its own long-standing comment saying so) but was missed when the
divider/`useResizableChatPanel` wiring went into the other two locations
earlier today. Same pattern applied here: a `.chat-panel-divider` inserted
before the chat wrapper (only while open), `panelRef` attached to the
wrapper, `flexBasis` driven by the shared hook — no new CSS needed, the
existing `.chat-panel-divider` rule (with its gap-cancelling margin) and
`.project-item-view__body`'s `gap: var(--space-loose)` already match the
other two locations exactly.

**2. The panel's open/closed state wasn't persisted at all** — user pointed
out that if the panel was open, it should stay open, not reset to closed on
the next reload or the next item you open. All three `useState(false)`s
(`discoveryOpen` in FilmWorkspaceView.tsx, `agentsOpen` in ProjectPanel.tsx,
`chatOpen` in ProjectItemView.tsx) replaced with a new
[usePersistedBoolean.ts](../../frontend/src/utils/usePersistedBoolean.ts)
hook (same shape as useTheme.ts/useTestMode.ts), all three keyed on one new
shared `CHAT_PANEL_OPEN_STORAGE_KEY` (co-located with the width key in
useResizableChatPanel.ts) — same "one shared preference across every docked
Agent panel" precedent the width key already established. Opening the panel
in one place now keeps it open everywhere, including across a reload.

No new ADR. Verified: `tsc -p tsconfig.app.json --noEmit`, `oxlint`, and
`npx vitest run` (77/77) all clean. Live-walked via the dev servers: opened
the panel from a project item's detail view, reloaded the page and
confirmed it landed open on the Project tab's own panel too (proving the
shared-key persistence works both across reload and across the three
locations); the resize divider rendered and behaved identically to the
already-verified locations.

## 03:30 — Tool-result cards: raw JSON dump replaced with a formatted summary + detail modal

User flagged (with a screenshot) that a mutation tool's result — e.g.
`propose_replacement` — rendered as `Applied: {"ok":true,"suggestedReplacement":
{"text":"...","justification":"..."}}`, a raw JSON dump, and suggested
formatting it and opening a modal on click to show what changed.

New `formatMutationResult(name, result, rubrics)` in
[ResearchChatPanel.tsx](../../frontend/src/components/ResearchChatPanel.tsx)
turns `update_rubric_score`'s and `propose_replacement`'s result objects
into a one-line human summary (`Set "Food aversion" to 8/10`, resolving the
rubric name via the `rubrics` prop already available one level up —
threaded into `ToolCallCard` as a new prop) for the inline card, and a
fuller breakdown (reusing the app's existing `.field` label/value pattern,
no new CSS) for a `Modal` that opens on click. Returns `null` for any
tool/shape it doesn't recognize, so the raw-JSON line stays as a defensive
fallback rather than silently showing nothing. `search_web`/
`describe_video_segment` results were already formatted (link list /
description text respectively), untouched.

One real bug caught and fixed before shipping: the `Modal` was first
rendered nested *inside* the now-clickable card div — since `Modal`'s own
backdrop closes on click, and the backdrop is a DOM descendant of the card,
a backdrop click bubbled up into the card's own `onClick` right after
closing it, instantly reopening the modal. Fixed by rendering the card and
the `Modal` as siblings inside a fragment instead of parent/child, so a
backdrop click has nothing to bubble into.

No new ADR. Verified: `tsc -p tsconfig.app.json --noEmit`, `oxlint`, and
`npx vitest run` (77/77) clean. Live-walked via the dev servers (test mode,
the mock chat agent's canned `update_rubric_score` call): confirmed the
card shows "Set "Food aversion" to 8/10" instead of raw JSON, clicking it
opens a modal with labeled Rubric/New score/New importance score rows, and
clicking the modal's close button actually closes it and stays closed (the
exact case the sibling-fragment fix targeted). Test session deleted
afterward.

Shipped straight to `main` per explicit user direction.

## 03:45 — The default research pass at project creation now auto-applies, skipping review

User: "Kick off agentic research on project creation?" in
[NewProjectModal.tsx](../../frontend/src/components/NewProjectModal.tsx) is
a checkbox the user explicitly opts into, but its results were landing in
the same accept/discard review queue as every other research run — this
morning's review-gating change (see the 20:15 entry two days back)
applied universally to `POST /api/projects/:id/research-runs`, with no way
for one specific caller to skip it. User wants exactly this one path —
the default pass at creation, already an explicit opt-in — to apply
straight to the items with no extra approval step; every other kickoff
(the in-panel "Kick off a research run" form, a chat-driven run) should
keep requiring review.

New optional `autoApply` flag on the kickoff route
([projects.ts](../../backend/src/routes/projects.ts)) and its client payload
type
([projectsApiClient.ts](../../frontend/src/api/projectsApiClient.ts)):
when true, `onBatchComplete` calls `projectItemStore.applyResearchResult`
directly for each result (same call `acceptResearchResult` in
researchResultActions.ts makes) instead of staging into
`run.pendingResults`. `NewProjectModal.tsx`'s kickoff call is the only
caller that sets it; every other kickoff (the in-panel form in
ResearchChatPanel.tsx, a chat-driven run) omits it, unchanged. New backend
test in
[projectsRoute.test.ts](../../backend/src/__tests__/projectsRoute.test.ts)
mirrors the existing "stages, doesn't touch items" test but with
`autoApply: true`, asserting `pendingResults` stays empty and the items are
patched (summary/shouldTranscreate/lastResearchedAt/action) immediately,
with no accept call in between.

No new ADR — additive within the existing kickoff route, not a new
architectural decision. Verified: `tsc -b`, `oxlint`, `npm run test`
(277 backend, 77 frontend, 12 integration) all clean. Live-walked the real
flow end-to-end via the dev servers: created a new project ("Japan") with
the checkbox checked and 2 selected detail rows, confirmed via a direct API
check that the run finished with `pendingResults: []` while both items
already carried real `summary`/`shouldTranscreate` values — then reloaded
the page and confirmed the Items table showed real Importance/AI
Assessment values immediately, no accept step, no "N results pending
review" badge ever appearing. Left the test project in place — this app has
no delete-project route to clean it up with (only items/rubrics/results
can be deleted individually), so it'll sit alongside the film's other
long-accumulated test projects.

Shipped straight to `main` per explicit user direction.

## 04:10 — Details-track scoping, rubrics UI rework, verdict coloring, kickoff picker fix

Four independent fixes/requests from one conversation.

**1. The scrub bar's Details track leaked every project's rows into every
other project.** [VideoScrubber.tsx](../../frontend/src/components/VideoScrubber.tsx)'s
`detailRows` prop was always the film-level `rows` from
[filmWorkspaceStore.ts](../../frontend/src/store/filmWorkspaceStore.ts) regardless
of which project (if any) was open — the track showed the same blocks no
matter which country tab you were on. `VideoScrubber` gained a `detailsLabel`
prop (defaults to the plain string `'Details'`); when a project is selected,
[FilmWorkspaceView.tsx](../../frontend/src/views/FilmWorkspaceView.tsx) now
passes `rows.filter((r) => r.id in itemStatusByRow)` (itemStatusByRow is
already keyed by `detailRowId` and already scoped per-project — see
`ProjectPanel.tsx`'s existing effect) instead of the full `rows` array, plus a
`<Flag/> Details` label instead of the bare word.

**2. Rubrics editor rework.** Both call sites
([NewProjectModal.tsx](../../frontend/src/components/NewProjectModal.tsx)'s
wizard step and [ProjectPanel.tsx](../../frontend/src/components/ProjectPanel.tsx)'s
Rubrics tab) share one [RubricsEditor.tsx](../../frontend/src/components/RubricsEditor.tsx),
which rendered each rubric as a cramped grid row of raw inputs. Rewrote it as
a stack of cards reusing `ProjectItemView.tsx`'s `.finding-card` visual
language (index circle, weight badge) — a rubric being authored should look
like the same object you later see scored. Also exposed `trendEligible` for
the first time: the field already round-tripped through
`createRubric`/`updateRubric` but had no UI control at all; added a checkbox
chip per card. Caught and fixed a related silent drop along the way —
`NewProjectModal.handleGenerateDefaultRubrics` copied `getDefaultRubrics()`
results into local draft state but dropped `trendEligible` in the process.

**3. "Your Verdict" didn't color like "AI Assessment" does.** User pointed
out the items table's verdict `<select>` stayed plain white text regardless
of value, unlike the colored badge look elsewhere. `actionColor()` (previously
a private helper inside `ProjectItemView.tsx`) moved to a new shared
[actionColor.ts](../../frontend/src/utils/actionColor.ts) util so
`ProjectPanel.tsx`'s `.action-picker` select could use the exact same
pending=yellow/accepted=green/rejected=red/need-research=accent mapping
`ProjectItemView.tsx`'s own verdict dropdown already used.

**4. The research-kickoff "Custom selection" table was broken.** User
screenshotted it: a raw 2-column `<table>` with no `<thead>` inside
`ResearchChatPanel.tsx`'s `KickoffForm` — under `table-layout: fixed` with
only 2 columns, the checkbox column ate ~50% of the row width, squeezing
subtitle text into single-line ellipsis truncation almost immediately. Worse,
an inline `style={{ maxHeight: 200 }}` on `.details-table-wrap` conflicted
with `.details-table-wrap--standalone .details-table-scroll`'s own
`max-height: 50vh` CSS rule — inline style always wins on the parent, but the
child scroll box could still grow past the parent's 200px cap (flexbox
doesn't clip overflowing children without an explicit `overflow`), which is
what produced the visible overlap with the "Test mode" checkbox underneath it
in the screenshot. Rewrote the table to match
[DetailRowPicker.tsx](../../frontend/src/components/DetailRowPicker.tsx)'s
shape (the one used in the project-creation modal the user held up as the
good reference): a real `<thead>` with a select-all checkbox, Time, Subtitle,
and Scene/segment description columns; removed the buggy inline maxHeight
entirely. New `.details-table__checkbox-col { width: 32px }` CSS rule (also
applied to `DetailRowPicker`'s own checkbox column, same latent issue there
just less visible with more columns diluting it) keeps the checkbox column
from eating disproportionate width regardless of how many other columns a
given picker table has. Also found and removed a genuinely dead duplicate
`.kickoff-modal { width: 440px }` rule — the second, later `.kickoff-modal`
definition (480px + max-height + overflow) already fully overrode it — and
widened that surviving rule to 640px (matching `.new-project-modal`) so the
new four-column table has room to actually show subtitle/scene text.

No new ADR — all four are additive UI/data-scoping fixes within existing
patterns, no architectural decisions. Verified: `tsc -p tsconfig.app.json
--noEmit` clean, `npm run test` (276 backend, 77 frontend, 12 integration)
all passing — no backend changes in this batch, so that suite is an
unaffected sanity check. **Not live-walked via the dev servers this time** —
unlike every other entry above, I did not stand up real film/project data
behind GCP ADC auth to click through these four changes in a running browser
before shipping; verification here is limited to type-checking and the
existing automated test suites. Worth a manual pass on your end, especially
the Details-track filtering (item 1) and the kickoff table (item 4), since
neither has direct test coverage.

Shipped straight to `main` per explicit user direction.

## 04:20 — Rubrics editor follow-up: full-width cards, bounded scroll in the wizard

User feedback on the rework above, from a screenshot of each surface: the
workspace Rubrics tab left a large dead margin down the right side (the
cards stopped at `.rubric-editor`'s leftover `max-width: 720px`, which
predated this session's rework and never got reconsidered when the layout
changed from a dense grid to full cards); separately, in the new-project
wizard's Rubrics step, a long rubric list pushed the hint text and Back/Next
buttons out of view, requiring a scroll to reach them.

Removed `.rubric-editor`'s `max-width: 720px` entirely — cards now fill
whatever width their container gives them in both usages. For the wizard
step specifically (explicitly scoped to that one step, not the modal or its
other three steps), wrapped `<RubricsEditor>` in a new `.rubric-editor-scroll`
box (`max-height: 48vh; overflow-y: auto`) — same "bounded internal scroll"
convention the Selected-details step's `DetailRowPicker` table already uses
via `.details-table-wrap--standalone`, just applied to a card list instead
of a table. The hint text above and the Back/Next buttons below sit outside
that box, so they never need to be scrolled past.

No new ADR. Verified: `tsc -p tsconfig.app.json --noEmit` and `npx vitest run`
(77/77) clean. Not live-walked via the dev servers — same caveat as the
04:10 entry above.

Shipped straight to `main` per explicit user direction.

## 04:30 — Reverted the trend-eligible checkbox; fixed inverted status colors; flag stacked in the scrub-bar label

Three more follow-ups.

**1. Removed the trend-eligible checkbox from RubricsEditor.** User pushed
back on it appearing at all. It was net-new UI added in the 04:10 rework
(the `trendEligible` field already existed on `Rubric`/round-tripped through
`createRubric`/`updateRubric`, but had no editor control before this
session) — not something requested, just something I added while reworking
the card layout. Reverted cleanly: dropped `trendEligible` off
`DraftRubric`, removed the checkbox markup, and reverted the two call sites
that only existed to plumb it (`NewProjectModal.emptyRubric()` and
`handleGenerateDefaultRubrics`, which would otherwise have failed
TypeScript's excess-property check against the now-narrower `DraftRubric`).
The field itself is untouched on the backend/`Rubric` type — still settable
via defaults, just not exposed as a manual toggle.

**2. `ProjectCard.tsx`'s per-project status pills had pending and
need-research backwards.** Screenshot showed "pending" rendering as plain
gray text (no color rule existed for `.project-card__stat--pending` at all —
it fell through to the base class's `var(--text-faint)`) while
"need research" rendered in warning-yellow. Every other place this same
four-way status renders (`actionColor.ts`, the scrub bar's Details-track
block coloring, `.details-table__row--*`) uses pending=warning/
accepted=success/rejected=danger/need-research=accent — this component alone
had pending uncolored and need-research wearing pending's color. Added the
missing `.project-card__stat--pending` rule (warning) and swapped
`--research` to accent, matching the convention everywhere else.

**3. The Details-track label's flag was illegibly tiny.** Screenshot showed
`<Flag/> Details` squeezed onto one line inside the scrub bar's 64px-wide
label gutter, using `.list-row__flag`'s 13px sizing meant for a normal-width
list row — at that width the flag read as a speck. Restructured to a
vertical stack (new `.scrubber-labels__flag-stack`, flex column, centered)
with a dedicated `.scrubber-labels__flag` at 20px instead of reusing
`.list-row__flag`, since the gutter has plenty of vertical room but almost
no horizontal room.

No new ADR — all three are corrections to changes made earlier today, not
new decisions. Verified: `tsc -p tsconfig.app.json --noEmit` and
`npx vitest run` (77/77) clean. Not live-walked via the dev servers.

Shipped straight to `main` per explicit user direction.

## 04:45 — Chat agent can now update AI Assessment; fixed a Details-track scoping regression from 04:10

**1. New `update_assessment` chat tool.** User asked for the Research chat
agent to be able to change the "AI Assessment" field (`shouldTranscreate` —
Fine As-Is / Needs Change, plus its Executive reason), not just rubric
scores (`update_rubric_score`) or a proposed replacement
(`propose_replacement`, which already flips `shouldTranscreate` to `true` as
a side effect but only in the "needs change + here's a draft" case). New
`UPDATE_ASSESSMENT_DECL` tool in
[researchChatAgent.ts](../../backend/src/services/researchChatAgent.ts)
takes `shouldTranscreate: boolean` + `summary: string` (required — an
assessment with no stated reason isn't useful) and calls
`projectItemStore.updateItem()`, which already accepted this exact patch
shape for the human-editable path in `ProjectItemView.tsx`; no store changes
needed. Tool description is explicit that this is for recording a verdict
independent of drafting a replacement (propose_replacement still handles the
"needs change, here's the fix" case on its own), and — same restriction as
every other tool here — never touches the human-only accepted/rejected/
pending/need-research `action` field. Frontend: `ResearchChatPanel.tsx` got
matching `toolStepLabel`/`formatMutationResult` cases so the tool call
renders as "Set AI Assessment to..." instead of falling back to a raw JSON
dump; the `item_patched` event needed no store changes since
`projectWorkspaceStore.applyChatEvent`'s no-`rubricId` branch already applies
any patch shape generically (the same path `propose_replacement` already
uses). New backend test mirrors the existing single-tool-call test, asserting
the tool_result/item_patched shape and that `shouldTranscreate`/`summary`
actually land on the item.

**2. Regression in the 04:10 Details-track scoping fix.** User caught it live:
navigating from a project's own tab back to the film-level "Details" tab
still showed the flag-labeled, project-scoped (now empty) track instead of
reverting to all film-level rows. Root cause: the filter condition was
`selectedProject ? ... : rows`, and `selectedProject` is derived from
`projectId` alone — which persists across tab switches — while
`itemStatusByRow` (what the filter actually keys on) is fed by `ProjectPanel`
only while it's mounted and gets explicitly cleared to `{}` on unmount (a
deliberate existing safeguard, per its own comment, against leaking stale
per-project coloring). So leaving the Project tab left `selectedProject`
truthy but `itemStatusByRow` empty — flag stayed, track went blank. Fixed by
introducing `viewingProject = tab === 'project' && Boolean(selectedProject)`
in [FilmWorkspaceView.tsx](../../frontend/src/views/FilmWorkspaceView.tsx)
and keying both the row filter and the label swap off that instead of
`selectedProject` alone.

No new ADR. Verified: `tsc -b` (backend) and `tsc -p tsconfig.app.json
--noEmit` (frontend) clean, `npm run test` — 277 backend (1 new), 77
frontend, 12 integration, all passing. Not live-walked via the dev servers,
per explicit instruction this round.

Shipped straight to `main` per explicit user direction.
