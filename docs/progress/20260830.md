# 2026-08-30

## Premiere-style zoomable timeline + horizontal transport bar

The user shared a Premiere Pro screenshot as a style reference and asked for the
workspace to feel more like a real NLE, specifically calling out: the timeline
scrubber being unreadable for a long (e.g. 2-hour) film with no way to zoom in, no
time ruler, no numeric time display anywhere, and the transport controls (play/
pause/etc.) sitting in a cramped vertical strip beside the video instead of a
horizontal bar underneath it like Premiere's Program Monitor. Planned this with the
user first (decisions: plain scroll-to-zoom with no modifier key, drop the
redundant "back to films" button from the new bar, fold picture-zoom into the same
bar) before implementing.

- **`frontend/src/components/VideoScrubber.tsx`** — full rewrite. Split the old
  single flat `.scrubber` div into a `.scrubber-viewport` (real `overflow-x: auto`,
  native scrollbar) containing a `.scrubber-track` whose width is `${zoom*100}%`.
  Every existing position (subtitle blocks, fill, playhead) was already expressed
  as a percentage of full duration, so it scales correctly with zoom for free —
  only the container width/scroll position needed new logic. Added:
  - A time ruler with "nice" tick spacing picked from a fixed candidate list
    (100ms…2hr) so labels never overlap regardless of zoom or clip length.
  - Scroll-wheel-to-zoom via a **native** `wheel` listener (`{ passive: false }`)
    attached in `useEffect`, since React 17+ registers `onWheel` passively at the
    root and won't reliably let `preventDefault()` stop page scroll otherwise.
    Zoom is centered on the cursor (classic zoom-to-pointer math).
  - `+`/`−`/Fit buttons next to the timeline for non-scroll access.
- **New `frontend/src/components/TransportBar.tsx`** replaces the deleted
  `VerticalToolbar.tsx`: one horizontal row (skip-back/play-pause/skip-forward,
  a `formatClock(current) / formatClock(duration)` readout, then picture-zoom
  controls) under `.video-stage` instead of beside it —
  `.workspace__panel--video` flipped from `flex-direction: row` to `column` to
  make this the actual layout, per the user's ask.
- `tsc --noEmit` clean, frontend suite 54/54, live-verified in a real browser
  (ticks, zoom-to-cursor, time readout, native panning).

## Follow-up feedback, same session

Three more requests landed while live-verifying the above:

1. **The auto-follow-playhead effect was actively fighting the zoom-to-cursor
   math** — the user zoomed into a region away from the current playhead and
   watched the view snap back to the playhead instead of staying where they'd
   zoomed. Root cause: a separate `useEffect` re-centering the viewport on
   `currentTimeMs` whenever `zoom` changed ran *after* the wheel handler's own
   cursor-centered scroll adjustment, silently overriding it. Fixed by removing
   that effect entirely, per the user's explicit "you don't have to jump to
   that."
2. **Sub-second granularity when zoomed in deep**: `formatClock()` only ever
   shows whole seconds (by design — nothing else in the app needs finer), so at
   zoom levels where the ruler's "nice step" picks a sub-second interval
   (100/250/500ms), every label was rendering as the same whole-second value.
   Added a ruler-local `formatTickLabel()` in `VideoScrubber.tsx` that appends
   `.mmm` only when `stepMs < 1000` (e.g. "00:01.250") — kept local rather than
   changing the shared `formatClock()`, since the transport bar and
   `AgentStatusPanel` deliberately don't want that precision.
3. **A second "bird's-eye" track for curated Details rows**, below the existing
   subtitle-entry track — mirroring a real NLE's multiple stacked tracks (the
   reference screenshot's V1/A1/A2/A3/A4 rows). `VideoScrubber` gained a
   `detailRows: DetailRow[]` prop and a second `.scrubber-content--details` row
   rendering one block per row (`startMs`/`endMs`, label falling back through
   `subtitleText` → `segmentDescription` → `gesture` → `notes`), accent-blue
   instead of the subtitle track's default panel styling so the two tracks read
   as visually distinct at a glance. Added a fixed-width `.scrubber-labels`
   column to the left of the scrollable viewport ("Subtitles" / "Details"),
   matching Premiere's fixed track-header column that stays put while the track
   content scrolls underneath it.
   - Also asked for the whole scrubber area to be **vertically resizable** via a
     drag handle, so a taller drag gives both tracks more room to read. Added
     `workspace__scrubber-divider` in `FilmWorkspaceView.tsx`, mirroring the
     exact same delta-based pointer-drag + `localStorage` persistence pattern
     already used by the existing left/right panel divider
     (`workspace.scrubberHeight` key, 84–400px clamp, defaults to 96px).
     `VideoScrubber`'s internal rows switched from fixed pixel heights to a flex
     column (`flex: 1 1 0` per row) so they grow/shrink proportionally with the
     container instead of clipping.
   - **Found via the fix itself, not a separate bug report**: the original
     zoom-state design read `zoom`/`viewportWidth` from the wheel handler's
     closure (re-subscribing the listener via the effect's dependency array on
     every change). Verified live that a burst of synthetic wheel events fired
     faster than React could re-render — realistic for an actual trackpad —
     only ever applied the *last* event instead of compounding, because every
     event in the burst computed from the same stale base. Fixed by moving
     `zoom`/`viewportWidth` into refs (`zoomRef`/`viewportWidthRef`) that the
     handler reads and writes synchronously, with `commitZoom()` syncing the
     ref back into React state for rendering. 20 synthetic wheel ticks with
     realistic spacing now compound correctly (100% → 1637%, matching
     `1.15^20`).
4. **"Those scrollbars look default as fuck"** — added a themed scrollbar rule
   (`scrollbar-color`/`scrollbar-width` for Firefox, `::-webkit-scrollbar*` for
   Chromium) applied globally via `*` in `index.css`, right after the base
   reset: thin, transparent track, `--border-strong`-colored thumb that lights
   up `--accent` blue on hover — replacing the native OS scrollbar chrome
   (most visible on the new timeline's horizontal scrollbar, but applies
   app-wide since a stock scrollbar anywhere would clash the same way).

- Live-verified all four fixes in a real browser (test mode): confirmed
  zooming into a region away from a mid-video playhead position no longer
  snaps back; confirmed sub-second tick labels ("00:00.100", "00:00.200", …)
  appear once zoomed in deep enough; confirmed the Details track renders both
  manually-added rows and the mock Discovery Agent's auto-generated rows,
  stacked below Subtitles with accent-blue blocks; confirmed dragging the new
  horizontal divider resizes the whole scrubber area and persists across a
  reload (`workspace.scrubberHeight` round-trip); confirmed the wheel-zoom
  compounding fix with 20 realistically-spaced synthetic wheel events;
  confirmed the themed scrollbar's computed `scrollbar-color` matches
  `--border-strong`. `tsc --noEmit` clean, frontend suite 54/54 throughout.
  Cleaned up: deleted the test film via a direct `DELETE` call, removed
  `frontend/public/tmp-test-assets/` and `backend/.data/mock-uploads/`.

## One more small tweak

The scrubber's `− 100% + Fit` zoom cluster was a horizontal row; the user asked
for it to be vertical instead — a one-line CSS change (`.scrubber-zoom`'s
`flex-direction: row` → `column` in `index.css`), no JSX changes needed since
the existing DOM order just restacks top-to-bottom. Verified via computed
style/bounding-rect in a real browser (`flexDirection: "column"`, increasing
`top` values for each button).

## Volume control

Added a mute button + slider to `TransportBar.tsx`, between the playback
controls and the time readout. `volume`/`muted` state now lives in
`FilmWorkspaceView.tsx` and is pushed onto the `<video>` element imperatively
via a `useEffect` (volume/muted aren't reactive JSX props, same pattern as
`currentTime` elsewhere in this view). Dragging the slider up while muted
auto-unmutes, matching every real media player's convention; the mute button
itself preserves the last volume rather than zeroing it. The slider itself is
custom-styled (`::-webkit-slider-thumb`/`::-moz-range-thumb`, an inline
`linear-gradient` fill tracking the current value) to match the dark theme
instead of a stock OS slider, following the same reasoning as the earlier
themed-scrollbar pass.

Live-verified: initial state (volume 1, unmuted); dragging the slider updates
`video.volume` in real time; clicking mute sets `video.muted` while preserving
the underlying volume value (slider visually drops to 0, icon → 🔇); dragging
the slider back up while muted auto-unmutes (icon → 🔊, `video.muted` false
again); the gradient fill's inline `background` matches the current value.
`tsc --noEmit` clean, frontend suite 54/54.

## Video upload: diskStorage instead of memoryStorage

The user is planning to import a real ~20-minute demo film and asked about
bandwidth/memory risk beforehand. `backend/src/routes/films.ts`'s video upload
route used `multer.memoryStorage()`, which buffers the *entire* file into the
Node process's RAM before the handler runs — fine for tiny test clips, a real
risk once real film-length files are involved (each upload spikes backend
memory by the full file size, on a single shared process).

- Switched to `multer.diskStorage()`, with `destination` pointed straight at
  `deps.mockUploadsDir` (created eagerly via `mkdirSync` at router-construction
  time, since diskStorage doesn't auto-create a missing destination the way
  the old lazy `mkdir` call did) and `filename` generating the same
  `${randomUUID()}${guessExtension(...)}` name the handler used to compute
  itself. This turned out to *simplify* the mock-mode branch, not just fix
  it — multer now writes the bytes directly to their final resting place, so
  the old `mkdir`/`writeFile(..., file.buffer)` dance is gone entirely; the
  handler just builds the URL from `file.filename`.
  - The real (bucket) branch still needs an in-memory `Buffer` for
    `videoBucketUploader.uploadBuffer()` — added a `readFile(file.path)` right
    before that call. This doesn't eliminate memory use for that path, but it
    shrinks the window a lot: the potentially-slow, attacker-controllable
    *network receive* no longer holds memory open (that's now a disk write),
    only a fast local disk read does. Didn't touch `videoBucketUploader.ts`
    itself — a true streaming-to-GCS variant exists as a pattern already
    (`uploadFromUrl`'s `createWriteStream` loop) but is more surface area than
    this pass needed, especially since mock mode is what's actually in use.
  - Added `keepFile`-gated cleanup (`rm(file.path, { force: true })` in a
    `finally`) so every path except mock-mode success — content-type
    rejection, real-upload success, real-upload failure — deletes the staged
    temp file instead of leaking it in `mockUploadsDir` forever.
- Live-verified in a real browser: a real upload lands at its final path with
  byte-for-byte the right size, and the film plays back
  (`video.duration`/`readyState` correct, no error); a deliberately-rejected
  non-video upload (`notavideo.txt`) gets its temp file cleaned up rather than
  orphaned. `tsc --noEmit` clean, backend suite 105/105, integration suite's
  one pre-existing unrelated failure (the stale `gs://` assertion, already
  flagged as a background task) is unchanged — the other 3 integration tests
  pass, confirming the upload→serve round trip still works end-to-end.

## Large video uploads: bypass Cloud Run's 32MB body limit

The user tried importing a real ~450MB film and got "Failed to fetch." Diagnosed
live: the exact same file uploaded fine via curl and a real browser `fetch()`
against the *local* backend — ruled out `maxVideoUploadBytes`, multer, basic
connectivity. The user confirmed the real attempt was against the **deployed
Cloud Run** backend. Confirmed via Google's current docs: Cloud Run enforces a
hard, non-configurable **32 MiB limit** on any HTTP/1.1 request carrying a
`Content-Length` header — a video `fetch()`/`FormData` upload always sets that
header, so anything over 32MB gets rejected by Google's front-end proxy before
the app ever sees it, which the browser reports as a bare connection failure.

Planned this with the user (`EnterPlanMode`) before implementing — walked
through the mechanism (resumable-upload sessions vs. signed URLs, chosen for
staying on ADC per `docs/adr/0003` rather than needing a new `signBlob` IAM
grant), then the user asked follow-ups about reliability and a progress bar and
asked to fold in full chunked/resumable uploads rather than a single-shot PUT.

- **Real (non-mock) video uploads now bypass Cloud Run entirely.** New
  `POST /api/films/upload-video/init` (tiny JSON body) mints a GCS
  resumable-upload session (`videoBucketUploader.createResumableUploadSession()`
  in `videoBucketUploader.ts`) and returns its `uploadUrl` plus the eventual
  `gs://` URI. The browser then PUTs the file **directly to
  storage.googleapis.com** in 8MiB chunks
  (new `frontend/src/api/resumableUpload.ts`, implementing Google's documented
  resumable-upload protocol by hand — no new dependency). A failed chunk
  triggers a status-check request (asks GCS how many bytes it actually has)
  and resumes from that offset rather than restarting, up to 3 attempts per
  chunk with backoff. `ImportFilmModal.tsx` shows upload progress via a
  `<progress>` bar driven by chunk-completion callbacks.
- **Mock mode is unchanged** — still posts through the existing multer/
  diskStorage route, by design (small test clips only, not real films; scope
  decision made with the user before implementing).
- Bumped `MAX_VIDEO_UPLOAD_BYTES` default 500MB → 2GB (`config/env.ts`) —
  matches the code's own pre-existing comment about real films running
  "hundreds of MB to a few GB."
- Also fixed the pre-existing stale `expect(videoUrl).toMatch(/^gs:\/\//)`
  assertion in `films.integration.test.ts` (flagged in `20260829.md`) while
  touching that file — mock mode actually returns an
  `http://.../mock-uploads/...` URL, not `gs://`.
- New ADR: `docs/adr/0024-direct-to-gcs-video-uploads.md`. New runbook step in
  `docs/runbook.md`: the video bucket needs a one-time CORS policy (`PUT` from
  the frontend's origins) since the browser now talks to
  `storage.googleapis.com` directly — kept as a manual step per the precedent
  `0011` already set for IAM grants, not scripted into `scripts/deploy.sh`.

**Verification**: `npm run test:backend`/`test:frontend`/`test:integration` all
green (113/61/5 tests — new coverage for `createResumableUploadSession`, the
`init` route, `resumableUpload.ts`'s chunk/retry/resume logic, and a new
integration test that drives the real chunk protocol over real TCP against a
fake-GCS stub server in-test). `tsc --noEmit` clean on both packages.

Live-verified against the **real** bucket, not just fakes: ran the CORS setup
step (with the user's explicit go-ahead, since it's a real infra change) and
uploaded the actual 450MB file. Ran a direct Node-based test of the same chunk
protocol against the real resumable session and it completed cleanly (all 54
chunks, byte-perfect, clean final response) — **wrongly** taken at the time as
proof the browser-side fix worked. It wasn't: Node's `fetch` doesn't send an
`Origin` header, so that test couldn't have hit a CORS problem even if one
existed, and one did (see below). Deleted the four ~429MB test objects this
session created from the real bucket afterward.

**Correction, same session**: shipped the fix to `main` (commit `025baba`),
then the user tried it for real in their own Chrome and hit "Failed to fetch"
again — this time with visible DevTools console output showing a genuine CORS
error against `storage.googleapis.com`, not a sandboxed-pane artifact as
earlier (wrongly) concluded above. Reproduced deterministically with per-chunk
logging in a real browser: **every intermediate `308 Resume Incomplete`
response from GCS carries a correct `Access-Control-Allow-Origin` header; the
completing `200` response never does** — confirmed directly with `curl`
(manually setting `Origin`, bypassing browser enforcement, purely to inspect
what GCS actually sends back). This is a genuine, apparently-undocumented GCS
platform behavior: a browser's `fetch()` can never read confirmation of a
resumable upload's own completion, success or failure, since the response
that would confirm it structurally omits the header a browser requires to read
it. See `docs/adr/0024`'s "Update" section for the full writeup.

Fix: `resumableUpload.ts` now treats a thrown error on the *final* chunk as
expected (not fatal) and resolves anyway — the bytes are sent regardless of
whether the response was readable. A new backend route,
`POST /api/films/upload-video/finalize`, independently confirms completion via
GCS object metadata (unaffected by browser CORS, since it's server-to-server)
and `filmsApiClient.uploadVideoFile` calls it after the resumable upload
returns. This same route also closes the size-cap gap flagged earlier in
conversation (a client could previously lie about the size it declares at
`/init` time, since GCS doesn't itself enforce a resumable session's total
against what was declared at creation) — `/finalize` checks the *actual*
uploaded size and deletes the object if it's over `maxVideoUploadBytes`.

Re-verified against the real bucket end-to-end via the actual browser
`uploadVideoFile()` call this time (not Node) — full progress to 100%, clean
`{ videoUrl }` result returned to the client, object confirmed byte-perfect
(449,236,799 bytes) via an independent `gcloud storage ls -l`. All three test
suites green again after updating `resumableUpload.test.ts` (the final-chunk
retry tests had to be rewritten around the new expected-throw-resolves
behavior) and adding coverage for the new route, `getObjectSizeBytes`,
`deleteObject`, and a new integration test that reproduces the exact
dropped-completion-response scenario against a fake server. `tsc --noEmit`
clean on both packages. Pushed the corrected fix to `main`.

## DetailsTable: edit modal, height overflow, and a column-resize bug

Three requests from the user after seeing the real film's 126-row Details
table for the first time (`DetailsTable.tsx`, `index.css`):

1. **Row click → modal, not inline-editable row.** Removed all the inline
   `isEditing ? <input> : text` branching in table cells; a row click now opens
   a `.modal-backdrop`/`.modal` dialog (same pattern as `ImportFilmModal`/
   `AgentKickoffPanel`) showing every field at full size — textareas for
   segment description/gesture/notes/custom columns instead of single-line
   inputs, since the whole point was viewing long AI-generated text in full.
   Added Escape-to-close, matching baseline modal expectations even though not
   explicitly asked. `handleDelete` now also closes the modal if the row being
   deleted is the one currently open in it (would otherwise crash on
   `rows.find(...)!` returning undefined).
2. **Table overflow pushing the horizontal scrollbar far down.** First pass
   capped `.details-table-scroll` at a flat `max-height: 60vh` — measured
   live and found this was actually *taller than the panel itself* (529px
   table vs. 488px panel), so the user immediately reported still having to
   scroll (now the panel, not the page) to reach the scrollbar. Real fix:
   `.details-table-wrap`/`.details-table-scroll` are now `flex: 1; min-height:
   0` inside `.workspace__panel--left`'s existing flex column, so the table
   area fills exactly whatever space is actually left after the heading and
   the two button rows below it — measured after the fix: panel
   `scrollHeight === clientHeight` (was 750 vs 488), i.e. the panel needs zero
   scroll now regardless of row count. Also made `.details-table th`
   `position: sticky` so the column headers stay visible while scrolling
   within the now-bounded table area.
3. **Column-resize drag "stuck" unless dragging very slowly.** Root cause:
   `ResizableTh` was defined *inside* `DetailsTable`'s render body — a new
   function/component identity on every render, so every `setColWidths`
   during a drag made React tear down and recreate the actual `<span>` DOM
   node, silently losing the `setPointerCapture()` that was supposed to keep
   tracking the cursor. Moved `ResizableTh` to module scope (handlers passed
   as props instead of closed over) so the DOM node — and its pointer capture
   — survives re-renders. Verified by dispatching real `PointerEvent`s
   directly (both one big fast jump and a smooth 10-step drag) and confirming
   the resulting column width matched the delta exactly in both cases; before
   the fix this class of bug wouldn't have worked for a fast drag.

Live-verified all three against the real 126-row "Disney's Inside Out" film:
opened the modal, edited and saved a field (round-tripped through the real
backend, then reverted the test edit afterward), scrolled the bounded table
with the header staying pinned, and confirmed the fast/multi-step drag
scenarios both land the resizer exactly where the cursor stopped.
`tsc --noEmit` and the full frontend suite (63/63) stayed clean throughout — no
existing `DetailsTable` test file existed to update (pre-existing gap, still
open, see TODOs).

## Discovery Agent: freeform (non-dialogue-anchored) findings

The user asked whether the Discovery Agent had been updated to not require every
finding to lock onto one specific subtitle entry — it hadn't; `docs/adr/0023`
(from a prior session) explicitly deferred this exact follow-up. Built it now.

- `discoveryAgent.ts`: Gemini's response schema drops `subtitleEntryId` in favor
  of the model supplying its own `startMs`/`endMs` (integer ms) per finding.
  Prompt now explicitly tells it findings don't need to match a subtitle line's
  boundaries — a visual gag/gesture/silent beat should get a range with no
  dialogue in it. `subtitleText` is now *derived* via the same
  `subtitleTextForRange()` the storage/UI layer already uses (see 0023), not
  copied from a matched entry. Invalid ranges (`endMs <= startMs`, negative)
  are dropped, same validation the freeform storage model itself enforces.
- `mockDiscoveryAgent.ts` got the same treatment for demoability without real
  Gemini calls: now proposes one freeform row (alongside its usual
  dialogue-anchored picks) in the gap after the first subtitle entry.
  **Caught a real bug in this mock logic via live-testing against the actual
  126-line "Disney's Inside Out" film**, not just the unit test's fixture: the
  first version hardcoded `subtitleText: ''` for the synthetic row and used a
  flat `+1500ms` fallback when there wasn't a "big enough" gap — on this real
  film, two lines sit only 82ms apart, so that fallback produced a row whose
  *declared* range actually overlapped the next line, while its *stored* text
  was still hardcoded empty. The mismatch surfaced as the edit modal showing
  the next line's real dialogue for a row the API said had none. Fixed by
  clamping the synthetic range to never extend past the next entry's start,
  and by deriving `subtitleText` the same honest way the real agent now does
  instead of assuming it — the unit test's own fixture had generous gaps and
  never would have exercised this.
- New `mockDiscoveryAgent.test.ts` (no prior test file existed for it) and
  updated `discoveryAgent.test.ts` for the new schema/validation. Updated
  `docs/adr/0023-freeform-detail-row-timing.md` with an "Update" section
  recording this as the deferred follow-up now built, rather than a new ADR
  number for what's really completing that same decision.

Live-verified end-to-end against the real film and the real (non-mock)
backend/API, not just unit tests: ran a mock discovery pass, confirmed the
freeform row's derived `subtitleText` was genuinely `''` and its range
genuinely didn't overlap either neighboring line, merged it into the film's
real Details table via the actual API, and confirmed the edit modal displays
it consistently (an em dash placeholder for the empty subtitle) rather than
recomputing something different from what was stored. Cleaned up all test
rows/jobs created during verification afterward. `npm run test:backend`
(129/129), `test:integration` (6/6), and `tsc --noEmit` all clean.

## Add-column modal with a description, fed to the Discovery Agent as context

The user asked for three things together: replace the `window.prompt`-based
"Add column" flow with a proper modal, add a description field to it, show
that description as a tooltip on the column header, and — importantly — the
description isn't just cosmetic, it's meant to genuinely reach the Discovery
Agent as context, so it had to actually be wired through, not just stored.

- `ColumnDoc` (`filmTypes.ts`/`apiClient.types.ts`) gains a `description: string`
  field. `detailRowsStore.addColumn()` (Firestore + in-memory) and the
  `POST /api/films/:id/columns` route both take it now (defaults to `''`).
- `DetailsTable.tsx`: `handleAddColumn`'s `window.prompt` is gone — replaced
  with a `.modal-backdrop`/`.modal` dialog (same pattern as the other modals
  in this file/app) with a Name input and a Description textarea, plus an
  explicit line telling the user the description is given to the AI discovery
  agents as context and needs to be accurate. `ResizableTh` gained an optional
  `title` prop; a custom column's header now shows its description as a native
  tooltip on hover.
- **The actual wiring, not just storage/UI**: `discoveryAgent.ts`'s
  `DiscoveryPassInput` gains `columnMeta?: Record<string, {name, description}>`;
  `buildSystemInstruction` now renders a custom column's real name and
  description in the prompt instead of falling back to its raw key (a
  pre-existing minor gap this touched in passing). `discoveryQueueWorker.ts`
  now takes `detailRowsStore` as a dep, fetches the film's current columns
  before each pass, and builds/passes `columnMeta` — the only place in the
  pipeline that actually knows a job's target columns' descriptions at run
  time. `mockDiscoveryAgent.ts` wasn't touched — its output is synthetic
  either way, not a real comprehension test.
- Updated `discoveryQueueWorker.test.ts`'s three worker constructions and the
  integration test's, all of which now need `detailRowsStore`. Added coverage
  in `discoveryAgent.test.ts` (description appears in the prompt; falls back
  to the raw key without a `columnMeta` entry) and extended the existing
  `filmsRoute.test.ts`/`detailRowsStore.test.ts` column tests for the new field.

Live-verified in the real browser against the real film: opened the modal,
filled in name + description, submitted, confirmed the new column's header
carries the description as its `title` attribute (native tooltip) exactly as
typed. Deleted the test column directly from Firestore afterward (no
delete-column endpoint exists in this app — a pre-existing gap, not something
this pass needed to add). `npm run test:backend` (132/132), `test:frontend`
(63/63), `test:integration` (6/6), and `tsc --noEmit` on both packages all
clean.

## 21:14 — Projects Pipeline Redesign: Firestore backend, film-first wizard, tool-calling research chat

Implemented the full plan at `curried-dazzling-puffin.md` (film-first project
creation, per-`ProjectItem` accept/reject status, and the tool-calling
"Claude Code edit mode" chat agent) in an isolated worktree, per the parent
session's explicit instruction. New ADR:
`docs/adr/0025-projects-firestore-pipeline-and-tool-calling-research-chat.md`
(supersedes 0013, extends 0012/0014) — read it for the full architectural
writeup; this entry summarizes what shipped and what's still open.

**Backend** (`backend/src/services/projectTypes.ts` + five new Firestore
stores — `projectStore.ts` rewritten off 0013's in-memory `Map`,
`projectRubricStore.ts`/`projectItemStore.ts`/`researchRunStore.ts`/
`chatSessionStore.ts` new, each with an in-memory fake + unit tests):
- Film-first creation only: `POST /api/films/:filmId/projects` (explicit
  `detailRowIds`, never every row unconditionally) replaces the deleted
  `POST /api/films/:id/create-project` bridge; new `projectItemImport.ts`
  preserves `startMs`/`endMs`/custom values and the FK back to the source
  DetailRow, closing 0013's flagged schema gap. `detailRowsToProjectItems.ts`
  deleted.
- Rewritten `routes/projects.ts`: project/rubric/item CRUD, a manual
  score-edit route, and research-run SSE (a rich per-batch kickoff stream
  plus a separate resumable `run_update` stream mirroring `films.ts`'s
  discovery-job stream per 0020). `importanceScore.ts` is the one shared
  weighted-score-to-sortable-number function used by both the batch path and
  the new chat tool-call path.
- **`researchChatAgent.ts`** — the first function-calling Gemini agent in
  this codebase. Multi-round streaming tool-calling loop (generalizes
  `discoveryAgent.ts`'s replay-and-append pattern), persisting
  `session.turns` after every round so a dropped connection's already-applied
  tool mutation never disagrees with the replayed history. Three v1 tools:
  `update_rubric_score`, `propose_replacement`, `search_web` (calls
  Parallel's REST API directly via `fetch()` — confirmed via Google's docs
  that the built-in `parallelAiSearch` tool cannot be combined with
  `functionDeclarations` on this app's Vertex AI + `gemini-2.5-flash` setup,
  HTTP 400). Deliberately no tool can change accepted/rejected/pending
  status — human-only by design. `mockResearchChatAgent.ts` scripts one
  canned `update_rubric_score` call through the same real `executeTool()`
  the live agent uses, so `testMode` demos a genuine persisted edit.
  Confirmed the streamed `functionCall` chunk shape against a real Vertex AI
  call with ADC before writing the loop (per the plan's smoke-test step);
  real Parallel API not smoke-tested (no `PARALLEL_API_KEY` configured in
  the sandbox), proceeded from the plan's cited REST shape instead.
- `routes/projectChat.ts`: chat-session CRUD + the SSE messages route.

**Frontend**: `ProjectsLibraryView.tsx` (grouped by country, one enriched
`GET` avoids N+1), `NewProjectWizardView.tsx` (4-step wizard — collapsed
into one file rather than 4 separate step components, a disclosed deviation
from the plan's file layout), `ProjectWorkspaceView.tsx` (filter tabs,
importance/time sort, inline action picker, manual-add-details and
research-kickoff modals), `DetailExpansionPanel.tsx` (item detail + docked
chat), `ResearchChatPanel.tsx` (session switcher, thread, and the
demo-critical distinct "🔍 Searching the web via Parallel…" step-card
treatment — real query while pending, real result cards once `tool_result`
lands). New `projectWorkspaceStore.ts` (zustand) replaces the old narrower
`projectStore.ts`. Deleted `NewProjectView.tsx`/`ProjectDetailView.tsx`/
`ProjectsListView.tsx` and their tests.

**Verification**: `npm test` — frontend 51/51, backend 175/175, integration
6/6, all green; `tsc --noEmit` clean on both packages.
`tests/integration/projects.integration.test.ts` rewritten end-to-end (real
backend, real frontend `apiClient`, only the research/chat agents faked —
CLAUDE.md's constraint held throughout): film-first creation → batch
research run → chat session → a message whose tool call actually patches
the `ProjectItem`, confirmed via a fresh `GET`.

Live-verified in a real browser against the **real** dev Firestore/backend
(not just fakes) — `testMode` on throughout, so no real Gemini/Parallel
calls: ran the new wizard against the existing "testing" film end-to-end,
watched a batch research run land in the workspace table with real
importance scores, opened a detail row's chat, sent a message, watched the
mock chat agent's tool call update a rubric score's badge **live** in the
same panel while the underlying table's importance score and sort order
updated in the background without a reload, confirmed all of it survives a
full page reload (Firestore-backed), and confirmed two chat sessions on the
same project keep independent histories. Found and fixed two real bugs this
way that no unit/integration test caught:
1. `projectItemStore.ts`'s score-upsert assigned `userNote: undefined`
   explicitly whenever nothing had set one yet — the in-memory test fake
   tolerates this, real Firestore's `.set()` throws on an explicit
   `undefined` field. Every chat-driven score edit silently failed against
   the real store until fixed (omit the key instead of assigning it
   undefined) — added a regression test asserting the key is genuinely
   absent, not just falsy.
2. `.details-table-wrap`/`.details-table-scroll`'s `flex: 1 1 0` layout only
   resolves to a real height inside a height-bounded flex ancestor (true for
   its one pre-existing usage). Every new Projects-pipeline usage dropped it
   into a plain auto-height flex column instead, where it silently collapsed
   to ~0px height and ate every click on the (invisible) checkboxes. New
   `.details-table-wrap--standalone` modifier fixes all five new call sites.
   jsdom doesn't reproduce this flex-collapse behavior, so no component test
   would have caught it either — exactly the class of bug live browser
   verification exists for.

Also discovered along the way: this worktree's `preview_start` tool spawned
dev servers rooted at the **main repo checkout**, not the worktree, despite
`.claude/launch.json` living in the worktree — silently serving stale code
for a while before being caught via a direct `fetch()` content check.
Worked around by starting both dev servers manually via backgrounded Bash
from the worktree's own cwd instead. Flagging this for whoever picks this
worktree up next: don't trust `preview_start` inside a worktree without
verifying which checkout it's actually serving from first.

**Deviations from the plan, all disclosed above/in the ADR**: the wizard's
4 steps live in one file, not 4; step 1 has no film picker (the film is
already fixed by the route's `:filmId`); the chat message route body gained
an undocumented-by-the-plan optional `itemId` field (necessary — see ADR);
`AgentRunningList.tsx` wasn't reused for research runs (its props are
`DiscoveryJobSummary`-shaped and don't generalize) — a small dedicated runs
list was written instead.

## Current open TODOs

- Everything listed in `20260829.md` still applies (no unit tests for
  `DetailsTable`/`FilmWorkspaceView`/`VideoScrubber`/`TransportBar`, real Gemini
  path unverified, `firestore.indexes.json` not deployed declaratively, resized
  column widths not persisted). Real video upload path is now verified (see
  above) — dropping that item from this carry-forward list.
- No automated test coverage was added for `VideoScrubber.tsx`'s zoom/pan math
  or `TransportBar.tsx` — this whole pass was verified only via live browser
  interaction (synthetic pointer/wheel events), same pre-existing gap as the
  rest of this view.
- The Details track's block coloring only distinguishes "default" vs "active"
  (accent vs warning) — it doesn't yet color-code by provenance
  (user-marked/agent-discovered/ai-assisted) the way `DetailsTable`'s status
  badges do. Not asked for; worth considering if the bird's-eye view needs to
  convey provenance at a glance later.
- **New from this session**: no component-level tests were added for the new
  Projects-pipeline views/components themselves (`NewProjectWizardView`,
  `ProjectWorkspaceView`, `ResearchChatPanel`, `DetailExpansionPanel`,
  `RubricsEditor`, `DetailRowPicker`, `ResearchKickoffPanel`,
  `ProjectsLibraryView`) — only `projectWorkspaceStore.ts`'s pure logic is
  unit-tested, plus the integration test's end-to-end coverage of the same
  logic. Time-boxed given the size of this plan; component rendering itself
  has no automated regression coverage yet.
- **New from this session**: the real (non-mock) path for both
  `researchChatAgent.ts` and `search_web`'s Parallel call is unverified — ADC
  was available and the streamed `functionCall` chunk shape was confirmed
  against a real Vertex AI call, but no `PARALLEL_API_KEY` was configured in
  this sandbox, so the actual Parallel REST response shape (search_id/
  results/excerpts) is only verified against the plan's cited docs, not a
  real call. Worth a real smoke test before this ships.
- **New from this session**: no `DELETE /api/projects/:id` route exists
  (`projectStore.deleteProject` is implemented but unwired) — same kind of
  gap as films' missing delete-column route. Test project cleanup this
  session went straight to Firestore via a throwaway script.
- **New from this session**: `ResearchRun`/chat-turn concurrency is no
  longer funneled through Discovery's single global queue (deliberate, see
  ADR 0025) — worth watching real Gemini/Vertex rate limits once this runs
  against the real (non-mock) agents for the first time.

## Fix: Projects UI didn't match the wireframe's actual layout

The Projects redesign above shipped functionally correct but structurally
wrong: project creation was a full-page wizard instead of a modal, the
project workspace was its own routed page (`/projects/:id`) instead of a
panel embedded in the Film workspace, and clicking a detail row opened a
modal instead of replacing the table in place. The user called this out
directly, re-pointed to the original wireframe, and confirmed (via one
targeted question) that a row click should swap the whole left panel from
table view to a focused item view, not expand inline or split permanently.

Corrected, no backend changes needed (data model/routes/agents from earlier
today are untouched):

- **`frontend/src/components/NewProjectModal.tsx`** (new, replaces the
  deleted `views/NewProjectWizardView.tsx`) — same 4-step content (Project
  info / Selected details / Rubrics / Confirmation), now a `.modal-backdrop`/
  `.modal` dialog matching the wireframe's consistently modal-sized mockups,
  opened via state from wherever a film's workspace needs it, not routed.
- **`frontend/src/components/ProjectPanel.tsx`** (new, replaces the deleted
  `views/ProjectWorkspaceView.tsx`) — the actual Project-tab content (header,
  Items/Rubrics tabs, filter tabs, sortable table, add-details/kickoff
  modals), now a plain component taking `projectId` as a prop instead of a
  routed page reading it from `useParams`.
- **`frontend/src/components/ProjectItemView.tsx`** (new, replaces the
  deleted `components/DetailExpansionPanel.tsx`) — same Start/End/Subtitle/
  rubric-blocks/chat content, no longer wrapped in `.modal-backdrop`/`.modal`.
  Renders in place of the table inside `ProjectPanel` when a row is open, has
  a "← Back to table" control, and now calls `onSeek(item.startMs)` on open/
  navigate so the workspace's video scrubs to match — parity with how
  `DetailsTable.tsx`'s row click already behaves, which it didn't have as a
  standalone modal.
- **`views/FilmWorkspaceView.tsx`**'s "Project" tab — was a bare "New project
  from this film" link; now fetches this film's projects (`GET /api/projects`
  filtered client-side by `sourceFilmId`, no new endpoint needed), shows a
  picker table when none is selected (matches the wireframe's per-film
  project list), and renders `ProjectPanel` in the same left panel used by
  Details/Progress when one is — the video/scrubber on the right and the tab
  bar stay mounted the whole time, exactly like the other two tabs. A
  `projectId` query param (alongside the existing `tab` param) makes a
  specific project deep-linkable.
- **`views/ProjectsLibraryView.tsx`** — row links now point to
  `/films/:sourceFilmId?tab=project&projectId=:id` instead of a standalone
  `/projects/:id` route, which no longer exists.
- **`App.tsx`** — removed the `/films/:filmId/projects/new` and
  `/projects/:id` routes entirely; `/projects` (the Library) is the only
  other Projects-related route now, everything else lives inside
  `/films/:id`.
- **`index.css`** — replaced `.project-item-modal*` (width/max-height rules
  meant for a dialog) with `.project-item-view*` (fills
  `.workspace__panel--left` via `flex: 1 1 0; min-height: 0`, same convention
  `.details-table-wrap` already uses — confirmed this was the right pattern
  by reading how `.workspace__panel` itself is a flex column before writing
  it, not by guessing), plus new `.project-panel__header`/`.new-project-modal`
  rules.

Live-verified against the real dev backend (test mode): Library → click a
project → lands inside the Film workspace with the Project tab active, video
mounted and playing the film, table showing Start/End/Importance/Subtitle/
Verdict/Action; clicked a row → table replaced in place by the item view
(Subtitle/scene/rubric block on the left, chat panel on the right, no modal
backdrop anywhere) and the video visibly scrubbed to that row's timestamp;
"← Back to table" and "← All projects for this film" both round-trip
correctly; "+ New Project" opens the 4-tab modal over the picker, not a page
navigation. Also had to kill several stale `vite`/`tsx` dev-server processes
left running from the earlier session's live-verification pass (on the
ports `preview_start` needed) before this pass could run against current
code. `npm test` (frontend 51/51, backend 175/175, integration 6/6) and
`tsc --noEmit` on both packages stayed green throughout — no backend files
touched by this fix, so those suites were a regression check, not new
coverage.
