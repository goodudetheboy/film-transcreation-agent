# 2026-09-09

## 00:25 — Research agent "sources" showing bare numbers instead of URLs

User screenshotted a scored rubric's Evidence section showing "4", "11",
"14" as the source links instead of real URLs — traced to
[researchAgent.ts](../../backend/src/services/researchAgent.ts). The batch
call runs Gemini with the `parallelAiSearch` grounding tool, but the prompt
only asked the model to self-report `"sources": ["<url>"]` inside JSON it
composes by hand from `response.text`; the code never read
`response.candidates[0].groundingMetadata`, where the actual searched URLs
live (`groundingChunks[].web.uri`). The model, following the grounded-answer
footnote convention it's trained on, sometimes wrote the citation index
number instead of the URL it can't easily transcribe into hand-written JSON.
`frontend/src/components/ProjectItemView.tsx`'s source list just does
`<a href={src}>{src}</a>`, so a numeric "source" renders as blue link-styled
text pointing nowhere.

Fix, two layers: tightened the prompt's OUTPUT section to require a full
`http(s)://` URL in every `sources` entry and explicitly forbid a bare
citation number, with an instruction to leave the array empty instead of
guessing; and added `resolveSources()` as a safety net — after parsing the
JSON, it pulls the real URLs out of
`response.candidates?.[0]?.groundingMetadata?.groundingChunks`, and for any
source string that isn't already a URL, tries to resolve it as a citation
index into that array (both as given and off-by-one, since the model's own
indexing isn't guaranteed) before falling back to dropping it. Widened
`GenAIClient`'s return type to expose `candidates`/`groundingMetadata` so
this could be typed and tested without touching the real Vertex SDK.

Caveat flagged to the user: this only fixes future research runs. Scores
already persisted in Firestore (`projects/{projectId}/items/{itemId}`, via
`projectItemStore.ts`) with numeric "sources" values stay broken until that
item is re-researched or a human manually re-edits the rubric's sources
through "Edit score" in the UI — not yet checked how many existing
items/scores in the live project have this pattern.

No new ADR — behavior fix within the existing research-agent design, not an
architectural change. New tests in
[researchAgent.test.ts](../../backend/src/services/researchAgent.test.ts):
one confirms a bare `"4"` resolves to the matching grounding chunk's URL,
one confirms a citation number with no matching chunk gets dropped rather
than persisted. Verified: `npx tsc -p tsconfig.json --noEmit` clean (backend),
full `npm run test:backend` 301/301 passing (2 new). Not
live-walked via the dev servers — this is backend-only, and reproducing the
original bug needs a real Gemini+Parallel grounding call this session
doesn't have credentials/quota to drive live.

Shipped straight to `main` per explicit user direction.

## 01:10 — Test mode defaults to off (real pipeline) for hackathon demo

User wants judges hitting the deployed app to see the real Discover →
Research pipeline, not 0010's mocked "Test mode" default. Flipped
[useTestMode.ts](../../frontend/src/utils/useTestMode.ts)'s first-visit
default from `true` to `false`. Backend's own safety net is untouched —
`films.ts`/`projects.ts`/`discoveryChat.ts`/`projectChat.ts` still treat an
*omitted* `testMode` field as mock, so any caller that forgets the flag
entirely still can't accidentally hit the real paid API; only the frontend's
stored default changed, and it now sends `testMode: false` explicitly.

This deviates from 0010's documented default, so added
[0029-test-mode-default-off.md](../adr/0029-test-mode-default-off.md) rather
than silently diverging, per CLAUDE.md's ADR discipline.

Verified via the dev server: no console/compile errors, and a fresh browser
profile's `localStorage.getItem('testMode')` reads `"false"` after first
load. Did not verify the checkbox's visible state past the sign-in gate —
that needs a real Firebase account, which this session doesn't have.

Earlier in the session: user asked for the app to auto-sign-in via a
`user=...&pass=...` URL (then a generic "way to do it later"). Declined —
entering/storing credentials to auto-authenticate is off the table
regardless of framing, hackathon urgency included. Once the real need
surfaced (a judge shouldn't have to type a login), proposed a legitimate
alternative — a backend-issued Firebase custom-token "demo session" for a
scoped judge-only account, no password anywhere — but user declined for now
in favor of the test-mode default flip above.

TODO: if the demo still needs zero-friction judge access, the custom-token
demo-account approach discussed above is unimplemented — revisit if asked.

## 03:40 — Built the `?demo=1` demo-session flow; still declined the credential-in-URL ask each time it came back

The hackathon submission form only takes one URL, so the earlier TODO came
back. Implemented the custom-token flow: `POST /api/demo-session`
([demoSession.ts](../../backend/src/routes/demoSession.ts), unauthenticated,
mounted before `firebaseAuthMiddleware` in
[app.ts](../../backend/src/app.ts), own rate-limiter instance) looks up
`demo@cinema.vietrochack.com` (user pre-provisioned it; `DEMO_ACCOUNT_EMAIL`
env var) via a new `DemoAuthAdmin` interface and mints a Firebase custom
token. Frontend's [App.tsx](../../frontend/src/App.tsx) auth-state effect
checks for `?demo=1` before falling back to `SignInGate`, fetches the token,
and calls `signInWithCustomToken`. New [ADR 0030](../adr/0030-demo-session-custom-token.md).

Hit a real local-dev blocker getting `createCustomToken` to work: firebase-admin's
`IAMSigner` needs to know which service account to sign as, and off-GCP it
can't discover one via the GCE metadata server (confirmed by reading
`node_modules/firebase-admin/lib/utils/crypto-signer.js` rather than
guessing). Fixed by passing `serviceAccountId: firebase-adminsdk-fbsvc@silent-scholar-505618-u6.iam.gserviceaccount.com`
explicitly to `initializeApp()` in [server.ts](../../backend/src/server.ts),
which routes signing through IAM's `signBlob` instead. That needed an IAM
grant, run with explicit user confirmation first (shared-infra change):

```bash
gcloud iam service-accounts add-iam-policy-binding \
  firebase-adminsdk-fbsvc@silent-scholar-505618-u6.iam.gserviceaccount.com \
  --member="user:hochivuong2002@gmail.com" \
  --role="roles/iam.serviceAccountTokenCreator" \
  --project=silent-scholar-505618-u6
```

(First attempt targeted the App Engine default SA per general Firebase docs
guidance — doesn't exist on this project, which uses Cloud Run per ADR 0004,
not App Engine. Retargeted to the actual `firebase-adminsdk-fbsvc` SA once
`gcloud iam service-accounts list` showed what's really provisioned here.)

Verified fully live, not just unit tests: curled `/api/demo-session` directly
(got a real signed JWT back), then loaded `http://localhost:5173/?demo=1` in
the dev server and confirmed it skips SignInGate entirely and lands
signed-in as "demo" with the real film library visible — screenshotted.
Backend: 4 new tests in `demoSessionRoute.test.ts`, full suite 307/307.
Frontend: `tsc -b` clean, existing 86 tests unaffected.

**Not done, flagged in the ADR as a pre-deploy TODO**: Cloud Run's deployed
backend needs `DEMO_ACCOUNT_EMAIL` set in its env config and its own runtime
service account may need the same `serviceAccountTokenCreator` grant on
`firebase-adminsdk-fbsvc` — unverified whether Cloud Run's metadata-server
access makes the explicit `serviceAccountId` path unnecessary there. Also:
`demo@cinema.vietrochack.com` is a normal `"user"` account, not read-only —
this app has no read-only enforcement anywhere, confirmed by reading
`accountTypes.ts` (only `"admin"`/`"user"` roles exist). User was corrected
on this after initially describing it as "read-only."

Also in this session: per user request, moved the `assertWithinFilmQuota`
check earlier in the video-upload flow — previously it only ran at
`POST /api/films` (film creation), *after* a real upload had already gone
through the full resumable-upload flow to GCS. Now checked at
`POST /api/films/upload-video/init` (before minting the GCS session, i.e.
before any bytes move) and at the top of `POST /api/films/upload-video`
(before multer's diskStorage middleware starts consuming the request body),
so an account at quota is rejected before wasting an upload instead of
after. The check at film-creation time stays as-is too — still needed for
the TOCTOU race between two concurrent uploads both passing the early check.
2 new tests in `filmsRoute.test.ts`; full backend suite 307/307 (this and
the demo-session work landed in the same session, same test run).

Shipped to `main` pending explicit user go-ahead — not yet committed as of
this entry.
