# 2026-08-25

## 02:20 — Research agent (Discovery→Research handoff) + Projects backend

- Built the Research agent per a teammate's handoff doc: `backend/src/services/researchAgent.ts`
  (real, Gemini via Vertex AI + Parallel Web Search grounding through the Marketplace
  subscription, no API key needed by default) and `mockResearchAgent.ts` (fake, reuses
  the same documented Inside Out broccoli case `mockDialogflowClient.ts` already cites).
  Batches items into chunks of 10, sequential (not concurrent) calls. Recorded in
  `docs/adr/0012-research-agent-gemini-parallel-grounding.md`.
- **Deviated from the handoff's literal sample code, deliberately**: added an optional
  `deps: { genAI? }` second parameter to `createResearchAgent`, matching this repo's
  existing `createApp(deps)` injection seam — the handoff's sample constructs
  `GoogleGenAI` inline with no way to fake it, but the "done" criteria require unit
  tests against a fake Gemini client. Confirmed with the user before writing it.
- Added `roles/aiplatform.user` + `aiplatform.googleapis.com` as new one-time GCP setup
  steps in `docs/runbook.md`, next to the existing Dialogflow role step.
- User then asked for a frontend to drive this — scoped it down through a live
  back-and-forth (voice-dictated, so noisier than usual) to: a `Project` groups one
  film's details (script line + video segment/scene description) under one target
  country; "Start Research" kicks off the Research agent; batch-by-batch progress
  should be visible as it runs; results should appear per item as they land. Explicit
  decisions made along the way: **in-memory backend store only** (no database yet —
  user floated Firestore, then reversed himself, twice, back to in-memory), items
  come from a fixture/manual-entry stand-in for Discovery for now (Discovery's real
  output shape doesn't have `id`/`sceneDescription`, and reshaping it is someone
  else's work), reuse the existing SSE pattern (0006/0009) for progress instead of
  polling, and a router library is fine to add for the frontend.
- Built the backend half: `backend/src/services/projectStore.ts` (in-memory
  `Map`-based store), `backend/src/routes/projects.ts` (create/list/get + a
  `POST .../research` SSE endpoint mirroring `analyze.ts`'s wire format, same
  test-mode mock/real switch as 0010), `backend/src/config/defaultRubrics.ts` (a
  clearly-marked placeholder rubric list — the real rubric set still isn't decided).
  Wired into `app.ts`'s existing `AppDeps` DI seam.
- Extended `ResearchAgent.researchBatch()` with an optional `onBatchComplete` callback
  (fired once per sequential batch) so the route can stream per-batch progress and
  persist partial results without changing the batching logic or breaking existing
  tests — callback is optional, old call sites unaffected.
- **Found and fixed a real bug before it shipped**: `passcodeMiddleware` only checked
  `req.body.passcode`, but the two new project GET endpoints are real GETs with no
  body by convention — a frontend `fetch()` GET wouldn't reliably deliver a passcode
  in the body. Fixed by also accepting it as a query param for GETs; POST/streaming
  requests are unaffected (still body-based). Covered by a new middleware test.
- Recorded the Projects/streaming decisions in
  `docs/adr/0013-projects-in-memory-store-and-research-streaming.md`.
- Extracted `frontend/src/api/sseStream.ts` (shared SSE frame-parsing loop, previously
  only inside `streamAnalyze`) so the new `projectsApiClient.ts`'s `streamResearch`
  doesn't duplicate it — refactor verified against the existing `apiClient.test.ts`
  suite (36/36 still passing, treats `streamAnalyze` as a black box).
- Added the minimal frontend API-client layer only (`projectsApiClient.ts` +
  `apiClient.types.ts` additions) — no UI/components yet, paused there deliberately
  to check in with the user before building screens. This was needed now (not after
  the UI) because `tests/integration/` must exercise the real frontend `apiClient`
  per CLAUDE.md's testing constraint — added
  `tests/integration/projects.integration.test.ts` +
  `tests/integration/helpers/fakeResearchAgent.ts` following the exact pattern of the
  existing Discovery integration test.
- Full suite green: 49 backend + 36 frontend + 5 integration = 90/90 passing,
  `tsc --noEmit` clean on both workspaces.
- **Ran the real manual test script once** (`backend/scripts/manual-research-test.ts`,
  `npm run test:research:manual` from `backend/`) against the actual Inside Out
  broccoli/Japan case — succeeded on the first try with real search-backed findings
  and real source URLs (Japan Times, Mashable, NHK World, etc.), confirming local ADC
  and IAM access already cover Vertex AI + Parallel grounding, no extra setup needed
  right now beyond what's already in place. Did not call it again, per instruction to
  keep real calls to one or two max and rely on mocks otherwise.

## 07:30 — Projects frontend, a real bug found live, commit + push to deploy

- Built the frontend half: added `react-router-dom` (new dependency), a header nav
  toggle (Analyze / Projects), and three new routed views — `ProjectsListView`,
  `NewProjectView` (repeatable script-line + scene-description rows standing in for
  Discovery), `ProjectDetailView` (item list with live status badges, "Start
  Research" button, batch-progress readout, expandable rows showing findings once a
  batch completes). New `frontend/src/store/projectStore.ts` (zustand) tracks
  per-item results as SSE `batch_done` events arrive. Extracted `AnalyzeView.tsx` out
  of `App.tsx` so both flows share the passcode gate/header/test-mode banner.
- **Found and fixed a real bug through live browser testing, not just unit tests**:
  after wiring everything up and clicking "Start Research" in test mode, items stayed
  stuck on "pending" forever even though the stream reported success. Root cause:
  `mockResearchAgent.ts` never called the new `onBatchComplete` callback at all — it
  only existed on the real agent. Fixed by batching the mock agent the same way as
  the real one (exported `BATCH_SIZE`/`chunk` from `researchAgent.ts` for reuse) and
  firing the callback per batch. Re-verified live: both a broccoli item (real
  finding) and a DMV item (correctly empty findings) now flip pending → done and
  expand to show the right content. This would not have been caught by the route
  unit tests alone, since those tests use a hand-written fake agent that already
  called the callback correctly — only exercising the actual `mockResearchAgent`
  live surfaced it.
- Verified responsively at mobile width (375px) — layout holds, no fixed grid forced
  on the new routed views (only `AnalyzeView`'s two-panel layout keeps the old grid,
  renamed to `.panel-grid` with its own media query).
- Confirmed no regression on the existing Analyze flow after the `sseStream.ts`
  extraction and `App.tsx` routing rewrite — ran it live end-to-end, same output as
  before (broccoli + DMV flagged lines).
- Full suite green after the fix: 57 frontend + 50 backend + 5 integration = 112/112,
  both workspaces' production builds (`tsc -b && vite build`, `tsc -p tsconfig.json`)
  succeed clean.
- Pulled a teammate's unrelated commit (`captioning_test.py`, fast-forwarded, no
  conflict) before pushing. Committed and pushed to `main` per explicit instruction,
  which triggers `.github/workflows/deploy.yml`'s automatic Cloud Run + Firebase
  Hosting deploy. Could not confirm the Actions run status directly (no `gh` CLI in
  this environment) — check the repo's Actions tab to confirm it succeeded.

## 07:45 — Mocked Films tab (Discovery stand-in), bridges the Discovery/Research schema gap

- User asked for mock frontend detail data plus a "Film" tab that mocks the Discovery
  agent: accepts a script + a (mocked, unfetched) video URL, shows already-processed
  films, and lets you spin up a Project from one. This finally resolves the schema
  gap flagged in `docs/adr/0013` — real Discovery's `FlaggedLine` output has no
  `id`/`sceneDescription`, but this new mocked one does, since it's defined fresh
  rather than reusing Discovery's real shape.
- Declined to fetch/scrape the actual Inside Out screenplay as literally requested —
  reproducing real dialogue lines from a copyrighted script into a committed fixture
  file would mean shipping substantial verbatim excerpts in the repo. Instead wrote
  `backend/src/fixtures/insideOutDetails.ts`: 15 original scene-description
  paraphrases based on the film's widely-known, publicly-discussed plot elements
  (Islands of Personality, Bing Bong, the dream-production studio, etc.), mostly
  visual-only (empty script line) — the same pattern already used for the one real
  documented case in this codebase (the broccoli→green-pepper swap).
- Backend: `filmStore.ts` (in-memory, same tradeoff as `projectStore.ts`), seeded
  with one pre-processed "Inside Out" film on startup so the tab isn't empty on
  first load. `routes/films.ts` — create/list/get, plus
  `POST /api/films/:id/create-project` (the actual Discovery→Research bridge:
  takes a target country, builds a `Project` straight from the film's details).
  "Discovery" here is entirely mocked — every film gets the same canned 15 details
  regardless of the script/video actually submitted, same "fixed content, not
  derived from input" convention as `mockDialogflowClient.ts`/`mockResearchAgent.ts`
  (0010). Wired into `app.ts`'s existing DI seam.
- Frontend: new "Films" nav tab, `FilmsListView`/`NewFilmView`/`FilmDetailView`.
  Extracted `httpHelpers.ts` (resolveBaseUrl/describeError/throwOnError) out of
  `projectsApiClient.ts` since `filmsApiClient.ts` would've been a third copy of the
  same code — refactor verified against the existing `projectsApiClient` test suite
  before adding the new client on top of it.
- Verified live end-to-end: seeded film shows 15 details correctly rendered → typed
  a target country → "Create Project" carried all 15 details into a new Project →
  "Start Research" batched them into 2 calls (10 + 5) → all 15 flipped to done.
  Confirms the full Discovery(mock) → Film → Project → Research pipeline shape
  works, not just each piece in isolation.
- Full suite green: 68 frontend + 62 backend + 5 integration = 135/135, both
  production builds clean.
- Not yet committed/pushed — checking with the user first, since the earlier
  push authorization was scoped to the Projects feature specifically, not
  open-ended for all further work this session.

## 08:05 — Fixed real research agent not being wired into the running server

- User hit `researchAgent not provided to createApp()` in the deployed/local app
  when clicking "Start Research" with test mode off. Root cause:
  `backend/src/server.ts` only ever constructed a real `dialogflowClient` and
  passed it to `createApp()` — it never constructed a real `researchAgent`, so
  `createApp()` fell back to its `notConfiguredResearchAgent` stub, which throws
  as soon as anything actually calls it with `testMode: false`. One-line-ish fix:
  `server.ts` now also calls `createResearchAgent(config)` and passes it through.
- Verified for real, not just by reading the diff: ran both dev servers, created a
  1-item project, toggled test mode off in Settings, clicked Start Research, and
  got back a real Gemini + Parallel Web Search result (correct rubric, real cited
  sources — NHK World, Mainichi, Asahi, Mashable, etc.) instead of the error.
  Confirms local ADC + Vertex AI access still work end-to-end through the actual
  app flow, not just the standalone manual test script from earlier today.
- Full suite still green after the fix (135/135), `tsc --noEmit` clean.

## 09:15 — Research agent redesign: exhaustive per-rubric scoring, verdict, suggested replacement

- User asked for a real behavior change: instead of Research selectively deciding which rubrics
  apply and gathering findings for those, every detail should be scored 0-10 against **every**
  rubric (exhaustive, no skipping), with a synthesized per-item verdict ("should this be
  transcreated, and why") and, when yes, one suggested replacement + justification. This directly
  contradicts `docs/adr/0012`'s explicit "evidence-gathering only, no ranking or fix" scope
  decision — not a silent drift, since 0012's Status is now `Superseded by 0014`, and
  `docs/adr/0014-research-agent-exhaustive-scoring-and-verdict.md` records the new decision, why,
  and the open consequences (Prioritization's/Proposal's future scope are both narrower now).
- Ran this as a full Plan-Mode cycle before touching code: an Explore agent mapped every current
  consumer of the old `RubricFinding`/`ResearchResult` shape (10 files, with exact line
  references), three clarifying questions confirmed score semantics (relevance/match-strength to
  the rubric's concern, not a "fit" score — higher = stronger risk signal) and the
  exhaustive-scoring-but-lazy-web-search tradeoff, then a Plan agent produced the detailed
  file-by-file design, which I verified against the two most detail-sensitive files myself
  (`researchAgent.ts`'s prompt/test string-slice dependency, `ProjectDetailView.tsx`'s exact JSX)
  before writing the final plan and getting it approved.
- Implemented in the planned order: `researchAgent.ts` (new `RubricScore`/`SuggestedReplacement`
  types, rewritten prompt, new snake_case→camelCase mapping) → its tests → `mockResearchAgent.ts`
  (now actually consumes the `rubrics` param it previously ignored, still surfaces the same real
  documented Inside Out/Japan broccoli case) → its tests → `projects.ts` (`totalFindings` →
  `totalRecommendedForChange`) → its route tests → the integration test fixture → frontend
  `apiClient.types.ts` → `ProjectDetailView.tsx` (new verdict block, suggested-replacement block,
  per-rubric score list with color-tiered scores, a compact verdict badge next to the existing
  pending/done status badge) → its CSS → its test.
- **Caught a real React bug while implementing, not just a schema mismatch**: my first draft placed
  a new `useMemo` (for a rubric-id → description lookup) *after* the component's existing
  conditional early returns (`if (!currentProject) return ...`), which violates the Rules of Hooks
  — hooks must run unconditionally, same order every render. Fixed by moving the `useMemo` above
  both early returns, with a `currentProject?.rubrics ?? []` guard inside it instead.
- Final sanity check: grepped the whole repo for `RubricFinding`, `.findings`, `reasonToChange`,
  `changeDirection`, `totalFindings` — zero hits, confirming nothing was missed.
- Full suite green: 137/137 (68 frontend + 64 backend + 5 integration), both production builds
  clean.
- **Live browser verification (test mode on/off, per the plan's own verification steps) was
  explicitly skipped this round** — the user interrupted the preview-server tool call twice and
  said to ship what's already built and tested. Automated coverage (unit + integration, including
  the real snake_case→camelCase mapping path) is solid, but nobody has actually looked at the new
  verdict/score/suggested-replacement UI rendering in a real browser yet, and the batch-size/output-
  truncation risk flagged in ADR 0014 (heavier per-call JSON under exhaustive scoring) has not been
  exercised against the real Gemini API under this new prompt. Flagging both as the first things to
  check next time the app is opened for real.

## Current open TODOs

- **New**: the exhaustive-scoring redesign has not been eyeballed in a real browser yet — verify
  the verdict block, per-rubric score list, and suggested-replacement block actually render
  correctly (test mode on), and run at least one real Gemini call (test mode off) to confirm the
  heavier per-call JSON parses without truncation under the new prompt (see ADR 0014's flagged
  batch-size risk).
- Whether the Cloud Run service account (as opposed to just local ADC) actually has
  `roles/aiplatform.user` granted is still unconfirmed — the successful real call above
  only proves local dev access works, not production. Runbook checklist item added but
  unchecked.
- Discovery's real output schema (`FlaggedLine`) still doesn't have `id`/
  `sceneDescription`, so there's no real bridge from Discovery to Research/Projects
  yet — items are manually entered for now. Flagged as someone else's schema work in
  `docs/adr/0013`, not attempted here.
- Prioritization and Proposal remain entirely unbuilt, per original instructions.

## 09:40 — Video Preprocessing tab: captioning feature integrated into the web UI

- Added a new "Video Preprocessing" tab alongside the existing "Script Analysis"
  tab, matching the user-supplied mockup: Video URL field, a disabled Script
  field ("coming soon" — not wired up yet per explicit request), "Submit for
  preprocessing" button, a loading state, numbered output rows
  ("N) line · video: start–end"), and a Country field + "Do research" button.
  "Do research" is a stub — visible but not wired to the analysis flow yet, per
  explicit scoping decision made during brainstorming.
- Wired to a **real backend endpoint**, not just a frontend mock: new
  `POST /api/preprocess-video`, following the exact `analyze.ts` pattern
  (passcode + rate-limit guarded, `testMode` picks real vs. mock client via
  `createApp(deps)`), but responds with plain JSON (`{lines}`) instead of SSE —
  one Gemini call, no per-line progressive reveal needed. Recorded in new
  `docs/adr/0012-video-captioning-via-gemini-vertex.md`.
- `backend/src/services/captioningClient.ts` — real client using `@google/genai`
  (Vertex AI), same GCP project as Dialogflow CX. Adapted the video-understanding
  technique proven out in `captioning_test.py` (gesture detection) to a new
  transcription+timecode prompt instead. New config: `CAPTIONING_LOCATION`
  (default `us-central1`), `CAPTIONING_MODEL_ID` (default `gemini-2.5-flash`).
- **Video input is a URL only** (e.g. a `gs://` URI) — no file upload UI, no
  GCS-upload step on the backend. This was an explicit scoping decision (asked
  the user directly) to keep this change bounded; uploading a local file to GCS
  first is left as a manual/out-of-band step for now.
- Built fully test-first: `mockCaptioningClient.test.ts`,
  `preprocessVideoRoute.test.ts` (6 cases mirroring `analyzeRoute.test.ts`'s
  shape — 400/401/200/500 plus the test-mode default/override matrix),
  `apiClient.test.ts` additions for `preprocessVideo()`, and a new
  `VideoPreprocessing.test.tsx` (6 cases). Watched each fail for the right
  reason (missing module / `TypeError: ... is not a function`) before
  implementing.
- Extended the integration suite per `CLAUDE.md`'s hard constraint (never mock
  the frontend→backend hop): `tests/integration/preprocessVideo.integration.test.ts`
  + `helpers/fakeCaptioningClient.ts`, driving the real `apiClient.preprocessVideo()`
  against a really-running backend with only the captioning client faked.
- Verified live in a real browser: typed a `gs://` URL, submitted in test mode,
  confirmed the real backend round-trip rendered the mock captioned lines in the
  new tab's output panel, laid out like the mockup.
- All suites green: backend 32/32 (was 26), frontend 43/45 (2 new files, 12 new
  tests; the 8 `PasscodeGate.test.tsx` failures are pre-existing —
  `localStorage` undefined in that file's test environment even on a clean
  `main`, unrelated to this change), integration 5/5 (was 3), `tsc --noEmit`
  clean in both workspaces.

## 09:47 — Verified the real (non-mock) captioning backend live, fixed a wiring bug

- User asked to confirm the real `captioningClient.ts` (not the mock) actually
  works, and provided a real test clip already in GCS:
  `gs://silent-scholar-505618-u6-clips/test.mp4`.
- **Found and fixed a real bug**: `backend/src/server.ts` (the actual dev/prod
  entrypoint) only ever constructed `dialogflowClient` and passed it to
  `createApp()` — `captioningClient` was never constructed there, so any
  `testMode: false` request fell through to `createApp()`'s
  `notConfiguredCaptioningClient` stub and threw `"captioningClient not
  provided to createApp()"`. Earlier verification only covered `app.ts`/tests,
  which inject deps directly — this gap in the actual server entrypoint wasn't
  caught until testing the real path live. Fixed with the same pattern already
  used for `dialogflowClient`: construct `createCaptioningClient(...)` from
  config and pass it in.
- Verified via direct `curl` (not just the browser) against the real backend
  with `testMode: false`: **HTTP 200, ~59s, 104 real transcribed dialogue
  lines with timecodes**, dynamically generated from the actual video — not
  the 3-line mock. Confirms local ADC auth to Vertex AI works, the
  `fileData.fileUri` (`gs://...`) approach works against a real GCS video, and
  `responseSchema` structured-output parsing holds up at 100+ items.
- Re-ran `tsc --noEmit` and the full backend suite (32/32) after the fix — both
  clean.

## 09:58 — Corrected the captioning prompt to actual gesture detection, fixed truncation risk

- User pointed out two real problems with the 09:47 verification: (1) the
  output was dialogue transcription, not the gesture/body-language detection
  `captioning_test.py` actually specifies (they pasted the exact prompt), and
  (2) the response looked like it could get cut off for a longer log.
- **Rewrote `captioningClient.ts` to mirror `captioning_test.py` exactly**:
  same `GESTURE_PROMPT` text, same fields per entry — renamed the type
  `CaptionedLine` → `GestureLog` (`timecode`, `gesture`, `character`,
  `narrativeLoad`, `backgroundNote`, camelCase versions of the Python
  dataclass fields) throughout backend, frontend, and integration-test code.
- **Fixed the truncation risk properly**, not just observed it: added
  `maxOutputTokens: 65536` to the Gemini call, plus an explicit check on
  `response.candidates[0].finishReason === 'MAX_TOKENS'` that throws a clear
  error instead of ever silently parsing/returning partial JSON. (The earlier
  104-line dialogue response had in fact completed fully — no truncation
  actually occurred there — but there was no ceiling or truncation detection
  at all, which was the real gap regardless of whether it had bitten yet.)
- Rebuilt test-first again: `mockCaptioningClient.test.ts` rewritten for the
  gesture shape (watched it fail with `undefined` for the old field names
  before fixing), `preprocessVideoRoute.test.ts` and `apiClient.test.ts`
  fixtures updated, `VideoPreprocessing.test.tsx`'s output-rendering test
  rewritten and watched fail (`findByText` timeout against the old
  `line`/`startTime`/`endTime` rendering) before rewriting the component to
  render `timecode`/`gesture`/`character`/`narrativeLoad`/`backgroundNote`.
- Re-verified live against the same real clip
  (`gs://silent-scholar-505618-u6-clips/test.mp4`) with `testMode: false`:
  **118 gesture entries, full JSON, HTTP 200, ~75s, no truncation** — correct
  shape this time (e.g. `{"timecode":"01:29","gesture":"Covers face with hand,
  peeking through fingers","character":"Woman","narrativeLoad":"High",
  "backgroundNote":""}`).
- Updated `docs/adr/0012` in place to record the correction (wrong prompt was
  a mistake, not a considered tradeoff) rather than leaving the stale version
  standing.
- All three suites green again after the rewrite: backend 32/32, frontend
  37/37 (excluding the pre-existing unrelated `PasscodeGate.test.tsx`
  failures), integration 5/5; `tsc --noEmit` clean in backend and frontend
  (the integration workspace's one `tsc` error, `ImportMeta.env`, is
  pre-existing on `main`, confirmed via `git stash`).

## Decisions recorded in ADRs today

`docs/adr/0012` (video captioning via Gemini/Vertex AI, single-shot JSON not SSE,
URL-only video input, "Do research" stubbed; updated same day to correct the
prompt to actual gesture detection and add truncation handling; later renumbered
to `0015` when reconciling with origin/main's own unrelated `0012` — see the
10:15 entry below).

## Current open TODOs

- `PasscodeGate.test.tsx`'s 8 failures (`localStorage` undefined in its test
  environment) are pre-existing on `main`, confirmed via `git stash` before
  starting this work — not caused by today's change, but still unfixed.
- No file-upload path for video yet — real (non-test-mode) use of the new tab
  requires the video to already be at a reachable URL (e.g. already uploaded to
  a GCS bucket in the project).
- "Do research" button is UI-only; wiring the captioned lines + country into the
  existing script-analysis flow is a deliberate follow-up, not done today.
- Everything else carried over from `docs/progress/20260815.md` (local ADC
  end-to-end confirmation, GitHub Actions WIF verification, provisional
  `FlaggedLine` schema, pitch doc TODOs, naming-consistency question) is still
  open and untouched by today's session.

## 10:15 — Reconciled with a diverged origin/main (Research agent + Films/Projects)

- Pushing this session's work hit a real conflict: `origin/main` had 5 new commits
  from a separate session (Research agent, Films/Projects flow, react-router
  migration) that had diverged from the same base this session started from —
  including a `docs/adr/0012` reused for a different decision, and a same-day
  `docs/progress/20260825.md` with entirely different entries.
- Per explicit instruction ("keep both, merge carefully"): rebased this session's
  single commit onto `origin/main` and resolved conflicts by hand rather than
  discarding either side or force-pushing over the other.
- Renumbered this session's ADR to avoid the `0012` collision (see
  `docs/adr/0015-video-captioning-via-gemini-vertex.md` — renamed from `0012`
  during this reconciliation).
- Dropped the standalone `CAPTIONING_LOCATION`/`CAPTIONING_MODEL_ID` config this
  session had added in favor of the other session's existing generic
  `geminiLocation`/`geminiModel` fields (already used by `researchAgent.ts`,
  same defaults) — same underlying need, no reason for two separate config knobs
  calling Vertex AI Gemini.
- Adapted `App.tsx`: the other session replaced the whole-app tab-switcher with
  `react-router-dom` routes + an `app-nav` header. Dropped this session's
  custom `.tabs`/`.tab` CSS and `view` state entirely; added `/gestures` as a
  new route + `NavLink` instead, pointing at the existing `VideoPreprocessing`
  component (wrapped in the other session's `.panel-grid` class instead of a
  bare fragment, matching `AnalyzeView.tsx`'s layout convention).
- `backend/src/app.ts` and `server.ts`: merged so both sessions' deps
  (`researchAgent`/`projectStore`/`filmStore` and `captioningClient`) are wired
  side by side into the same `createApp(deps)`/`AppDeps`.
- Full suite re-verified green after reconciliation (see next entry).

## Decisions recorded in ADRs today (updated)

`docs/adr/0012` (Research agent, Gemini + Parallel grounding — from the other
session), `0013` (Projects in-memory store + research streaming), `0014`
(Research agent redesign — exhaustive scoring + verdict), `0015` (video
captioning via Gemini/Vertex AI — this session's ADR, renumbered from `0012`
during reconciliation to avoid the collision).
